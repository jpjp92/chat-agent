/**
 * 재구성 요청 턴 규칙 — 순수.
 *
 * "표로 정리해줘"·"요약해줘"·"비교해줘" 처럼 **직전 답변을 다른 형식으로 다시 보여달라**는 턴.
 * 이런 턴은 툴도 검색도 없이 도는 경우가 많아 모델이 추가한 내용을 검증할 장치가 없다.
 *
 * 🔴 `prompt.ts` 의 정적 `[REFORMAT REQUESTS]` 규칙만으로는 안 먹혔다 — 직전 턴이 **빈 응답**
 * 이었는데 제품 4개짜리 표를 만들어낸 사례가 있다(DEV_260815_DEPLOY_CHECK). 그래서 해당 턴에만
 * 강하게 못 박는 이 블록이 따로 있다. **같은 주제가 두 층에 있는 건 의도된 중복**이고,
 * 재배치할 때 한쪽만 지우면 그 회귀가 돌아온다(PLAN_PROMPT_LAYERING §7-3).
 *
 * 2026-09-24 에 `prompt-assembly.ts` 인라인에서 떼어냈다. 문구는 그대로다.
 */
export const buildReformatRules = (): string =>
    `[재구성 요청 처리 규칙 — 이번 턴]\n- 이 턴은 직전 답변을 **다른 형식으로 다시 보여달라**는 요청입니다. 형식만 바꾸고 내용은 그대로 보존하세요.\n- 직전 답변에 없던 항목·제품명·브랜드·제조사·수치·날짜를 **추가하지 마세요.** 표의 행 수가 직전 답변의 항목 수보다 많아지면 잘못된 것입니다.\n- 칸을 채울 정보가 없으면 비워두거나 열을 빼세요. 기억으로 채우지 마세요.\n- **직전 답변이 비어 있거나 실패했다면 재구성할 대상이 없다고 말하세요.** 없는 원본을 상상해서 만들지 마세요.\n- 사용자가 명시적으로 "더 추가해서"·"다른 것도"라고 요청한 경우에만 예외입니다.`;
