/**
 * 외부 사이트로 나갈 때 쓰는 브라우저 User-Agent — **한 곳에서만 정한다.**
 *
 * 🔴 왜 상수로 뺐나 (2026-08-22 실측):
 *    네 곳에 하드코딩돼 있었고 값이 셋으로 갈려 있었다 (`Chrome/120` ×3, `Chrome/124` ×1).
 *    그 사이 `Chrome/120` 은 **2023-12 릴리스**라 2년 넘게 묵어 있었고,
 *    **GeekNews 가 이 UA 를 403 으로 거부하고 있었다**:
 *
 *      Chrome/120 → 403 ·     10 bytes
 *      Chrome/141 → 200 · 43,009 bytes  (같은 URL, 같은 나머지 헤더, 13ms)
 *
 *    즉 사이트가 우리를 막은 게 아니라 **낡은 UA 를 봇 시그니처로 막고 있었다.**
 *    그 결과 무료 경로(direct fetch)가 실패하고 **유료 스크래퍼로 떠넘겨졌다** —
 *    2026-08-22 ScrapingBee 월 쿼터 소진에 이것이 얼마나 기여했는지는 별도 측정 대상이다.
 *
 * ⚠️ **이 값은 시간이 지나면 조용히 썩는다.** 코드가 바뀌지 않아도 세상이 움직여서 틀려진다.
 *    URL 요약이 특정 사이트에서만 403 을 받기 시작하면 **여기를 가장 먼저 의심할 것.**
 *    판별법: 같은 URL 을 최신 Chrome UA 로 한 번 더 쳐 본다. 상태가 갈리면 원인은 이것이다.
 *
 * 📌 실측으로 통과가 확인된 값만 적는다. 최신처럼 보이는 숫자를 넣는 것이 아니라,
 *    **실제로 200 을 받은 값**을 적는다(위 GeekNews 대조).
 */
export const BROWSER_UA =
    'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/141.0.0.0 Safari/537.36';
