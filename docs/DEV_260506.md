# DEV_260506 — 외부 API 통합 기획

> **작성일**: 2026-05-06  
> **작성자**: 개발팀  
> **관련 파일**: `docs/TODO.md` (🌐 외부 API 통합 계획 섹션)

---

## 1. 배경 및 목표

### 배경
- 현재 chat-agent는 LLM(Gemini) 단독 답변에 의존
- 실시간 공공데이터(약국 위치, 병원 진료시간, 문화행사 등) 조회 불가
- 레퍼런스 코드(`reference/chat_w_AI/app.py`, `app2.py`) 분석으로 통합 패턴 확인

### 목표
- **실시간 공공데이터**를 LLM 답변에 통합 → 구조화된 카드 UI로 렌더링
- **7개 API** 순차 통합 (Phase 1~4)
- 기존 `DrugRenderer` 패턴 재사용 → 구현 비용 최소화

### API 키 현황 (모두 발급 완료 · 테스트 PASS)
| API | 환경변수 | 상태 |
|-----|---------|------|
| 의약품 정보 (공공데이터포털) | `DRUG_API_KEY` | ✅ 운영중 |
| arXiv | 키 없음 | ✅ 테스트 완료 |
| PubMed (NCBI) | `NCBI_KEY` | ✅ 테스트 완료 |
| 서울 문화행사 | `CULTURE_API_KEY` | ✅ 테스트 완료 |
| 서울 약국 | `DRUG_STORE_KEY` | ✅ 테스트 완료 |
| 서울 병원 | `HOSPITAL_KEY` | ✅ 테스트 완료 |
| NEIS 학교정보 | `EDU_KEY` | ✅ 테스트 완료 |
| 국가법령정보 | OC=`jpjp9202` | ✅ 테스트 완료 |

---

## 2. 전체 시스템 구조

### 데이터 흐름
```
사용자 메시지
    ↓
router.ts (gemini-2.5-flash-lite)
    → intent 분류 (의도 기반 LLM 판단)
    ↓
generator.ts
    → 해당 Tool 호출
    → API 응답 파싱
    → json:<type> 블록 출력
    ↓
ChatMessage.tsx
    → 블록 파서
    → <TypeRenderer> 렌더링
```

### 현재 구조 (운영중)
```
drug_id    → VisionNode → DrugIdentifyTool  → DrugRenderer
drug_info  → Generator  → DrugInfoTool      → DrugRenderer
```

### 추가 예정 구조
```
pharmacy_search  → Generator → PharmacyTool  → PharmacyRenderer
hospital_search  → Generator → HospitalTool  → HospitalRenderer
paper_search     → Generator → PaperTool     → PaperRenderer
culture_event    → Generator → CultureTool   → CultureRenderer
school_search    → Generator → SchoolTool    → SchoolRenderer
law_search       → Generator → LawTool       → LawRenderer
```

---

## 3. Intent 분류 설계

### 3-1. 분류 주체
- **gemini-2.5-flash-lite** (현재 router.ts L82)
- `temperature: 0`, JSON 응답 강제
- LLM 실패 시 → 키워드 heuristic fallback

### 3-2. 핵심 원칙 — "이동 의도 vs 정보 의도"

```
사용자가 실제로 이동해야 하는가?
    YES → pharmacy_search / hospital_search
    NO  → drug_info / medical_qa
```

### 3-3. 충돌 케이스 및 기준

| 메시지 예시 | 올바른 분류 | 판단 근거 |
|------------|-----------|---------|
| `"타이레놀 부작용"` | `drug_info` | 약 자체 정보 요청 |
| `"강남구 약국"` | `pharmacy_search` | 위치 탐색 |
| `"두통약 파는 약국 어디야"` | `pharmacy_search` | 이동 의도 우선 |
| `"약국에서 처방 없이 살 수 있는 약"` | `drug_info` | 약 정보 질의 |
| `"강남구 병원"` | `hospital_search` | 위치 탐색 |
| `"응급실 가야 할까?"` | `medical_qa` | 의학 판단 요청 |
| `"지금 열린 응급실 어디야"` | `hospital_search` | 이동 의도 |
| `"병원 드라마 추천"` | `general` | 엔터테인먼트 |
| `"내과 증상이 뭐야"` | `medical_qa` | 의학 지식 |
| `"내과 있는 병원 찾아줘"` | `hospital_search` | 이동 의도 |

### 3-4. LLM 프롬프트 설계 (의도 설명 방식)

기존 프롬프트 패턴 (keyword 나열) → **의도 + 예시 + NOT 패턴** 3요소 구성으로 변경

```
"drug_info"       : 약품 자체 정보 (성분·효능·부작용·복용법).
                    약국 위치 찾기가 아님.
                    Ex) "타이레놀 부작용", "두통약 추천", "아스피린 성분"
                    NOT: "약국 어디야"(→ pharmacy_search)

"pharmacy_search" : 사용자가 실제 약국에 방문하려는 의도.
                    지역명·영업시간·전화번호가 목적.
                    Ex) "강남구 약국", "지금 여는 약국", "두통약 파는 약국 어디야"
                    NOT: "두통약 성분"(→ drug_info)

"medical_qa"      : 증상·질병·치료법 등 의학 지식 질의.
                    병원을 찾는 것이 아닌 정보 요청.
                    Ex) "두통 원인", "혈압약 먹어도 돼?", "이 증상 뭐야"
                    NOT: "두통 병원 어디야"(→ hospital_search)

"hospital_search" : 사용자가 실제 병원에 방문하려는 의도.
                    지역명·진료시간·응급실 여부가 목적.
                    Ex) "강남구 병원", "응급실 어디야", "내과 병원 찾아줘"
                    NOT: "내과 가야 할 증상"(→ medical_qa)
                    NOT: "병원 드라마 추천"(→ general)
```

### 3-5. Fast-path 정책 (LLM 생략)
- YouTube URL → `general` 유지
- 이미지 + 비약품 → `general` 유지
- 약국·병원은 **fast-path 없음** — LLM 판단에 완전 위임
  - 이유: "약국"이라는 단어가 포함돼도 의도가 다양 (fast-path 시 오분류 위험)

---

## 4. 카드 UI 설계

### 4-1. 확정된 디자인
- **파일**: `scripts/card-preview.html` v2
- **스타일**: 기존 앱 글래스모피즘 시스템 일관성 유지
  - `glass` / `glass-light` / `dark:bg-white/[0.06]`
  - `rounded-[1.75rem]`, `backdrop-blur-xl`

### 4-2. 약국 카드 포함 정보
| 필드 | API 필드 | 표시 방식 |
|------|---------|---------|
| 약국명 | `DUTYNAME` | 헤더 타이틀 |
| 주소 | `DUTYADDR` | 헤더 서브텍스트 |
| 영업상태 | 계산 (현재 KST) | 🟢 영업중 / 🔴 영업종료 배지 |
| 오늘 영업시간 | `DUTYTIME{요일}S/C` | 상단 강조 행 |
| 전 요일 + 공휴일 | `DUTYTIME1~8S/C` | 8칸 그리드 |
| 전화 | `DUTYTEL1` | 탭 클릭 → 전화 앱 연결 |
| 지도 링크 | `WGS84LAT/LON` | 카카오지도 링크 버튼 |

### 4-3. 병원 카드 포함 정보
| 필드 | API 필드 | 표시 방식 |
|------|---------|---------|
| 병원명 | `DUTYNAME` | 헤더 타이틀 |
| 병원종류 | `DUTYDIVNAM` | 배지 (종합병원/의원 등) |
| 응급실 | `DUTYERYN`, `DUTYEMCLSNAME` | 🚑 배지 |
| 주소 | `DUTYADDR` | 헤더 서브텍스트 |
| 진료상태 | 계산 (현재 KST) | 🟢 진료중 / 🔴 진료종료 배지 |
| 오늘 진료시간 | `DUTYTIME{요일}S/C` | 상단 강조 행 |
| 전 요일 + 공휴일 | `DUTYTIME1~8S/C` | 8칸 그리드 |
| 진료과목 | `DUTYINF` | 태그 목록 |
| 비고 | `DUTYETC` | 하단 안내 텍스트 |
| 전화 (일반) | `DUTYTEL1` | 탭 클릭 → 전화 앱 연결 |
| 전화 (응급) | `DUTYTEL3` | 별도 응급전화 버튼 |
| 지도 링크 | `WGS84LAT/LON` | 카카오지도 링크 버튼 |

### 4-4. 목록 모드 (검색 결과 여러 건)
- 약국/병원 모두 **목록형 + 단일 상세 카드** 2가지 모드
- 목록 아이템: 이름 / 주소 / 영업상태 배지 / 오늘 운영시간
- 페이지네이션: 5건씩 표시, 더보기 버튼

---

## 5. API 데이터 처리 주의사항

### 서울시 OpenAPI 공통
- 전체 건수: 약국 5,358건 / 병원 22,239건
- **페이지 전략**: 100건 로드 → 구 단위 클라이언트 필터
  - 구 지정 시: 500건 로드 (레퍼런스 패턴)
  - 구 미지정 시: 100건만 (전체 탐색 방지)
- **KST 기준 영업상태 계산** 필수 (서버 타임존 UTC 주의)
- 요일 인덱스 매핑: `DUTYTIME1S/C` = 월, `DUTYTIME7S/C` = 일, `DUTYTIME8S/C` = 공휴일

### GPS 좌표 활용
```
카카오지도 링크 포맷:
https://map.kakao.com/link/map/{이름},{위도},{경도}
```

### 진료과목 파싱 (`DUTYINF`)
- 자유형식 텍스트: `"내과, 외과, 정형외과, 신경과..."`
- 쉼표·`·`·줄바꿈으로 split → 태그 배열 변환

---

## 6. 구현 파일 목록

### Phase 1 — 약국 + 병원

#### 신규 파일
| 파일 | 역할 |
|------|------|
| `api/_lib/agent/tools/pharmacy-tool.ts` | 약국 API 호출 + 필터링 + 영업상태 |
| `api/_lib/agent/tools/hospital-tool.ts` | 병원 API 호출 + 필터링 + 진료상태 |
| `components/PharmacyRenderer.tsx` | 약국 카드 컴포넌트 (목록/단일) |
| `components/HospitalRenderer.tsx` | 병원 카드 컴포넌트 (목록/단일) |

#### 수정 파일
| 파일 | 변경 내용 |
|------|---------|
| `api/_lib/agent/state.ts` | `IntentType`에 `"pharmacy_search"`, `"hospital_search"` 추가 |
| `api/_lib/agent/nodes/router.ts` | LLM 프롬프트 카테고리 확장 (의도+예시+NOT 패턴) |
| `api/_lib/agent/nodes/generator.ts` | `LANGCHAIN_INTENTS` + `allTools` 분기 추가 |
| `api/_lib/agent/prompt.ts` | intent focus hint 추가 |
| `components/ChatMessage.tsx` | `json:pharmacy`, `json:hospital` 블록 파서 추가 |

### Phase 2~4 (약국·병원 완료 후 동일 패턴 반복)
- `paper-tool.ts` + `PaperRenderer.tsx`
- `culture-tool.ts` + `CultureRenderer.tsx`
- `school-tool.ts` + `SchoolRenderer.tsx`
- `law-tool.ts` + `LawRenderer.tsx`

---

## 7. 응답 JSON 포맷 스펙

### 약국 (`json:pharmacy`)
```json
{
  "mode": "list",
  "district": "강남구",
  "total": 11,
  "items": [
    {
      "name": "365강남역약국",
      "address": "서울특별시 강남구 강남대로 지하396",
      "phone": "02-554-5628",
      "status": "open",
      "todayHours": "07:30~23:00",
      "hours": {
        "mon": "09:00~20:00",
        "tue": "07:30~23:00",
        "wed": "09:00~20:00",
        "thu": "09:00~20:00",
        "fri": "09:00~20:00",
        "sat": "09:00~14:00",
        "sun": null,
        "holiday": "10:00~14:00"
      },
      "lat": 37.498095,
      "lng": 127.027610
    }
  ]
}
```

### 병원 (`json:hospital`)
```json
{
  "mode": "list",
  "district": "강남구",
  "total": 22,
  "items": [
    {
      "name": "강남성심병원",
      "type": "종합병원",
      "address": "서울특별시 강남구 도산대로 123",
      "phone": "02-1234-5678",
      "emergencyPhone": "02-1234-5679",
      "hasEmergency": true,
      "emergencyClass": "지역응급의료센터",
      "status": "closed",
      "todayHours": "09:00~17:30",
      "hours": {
        "mon": "09:00~17:30",
        "tue": "09:00~17:30",
        "wed": "09:00~17:30",
        "thu": "09:00~17:30",
        "fri": "09:00~17:30",
        "sat": "09:00~13:00",
        "sun": null,
        "holiday": "emergency_only"
      },
      "departments": ["내과", "외과", "정형외과", "응급의학과"],
      "note": "야간 응급진료 가능",
      "lat": 37.519017,
      "lng": 127.039674
    }
  ]
}
```

---

## 8. 구현 순서 (Phase 1 세부)

```
Step 1: state.ts
  └─ IntentType에 "pharmacy_search", "hospital_search" 추가

Step 2: router.ts
  └─ LLM 프롬프트 카테고리 11개로 확장
  └─ validIntents 배열 업데이트

Step 3: pharmacy-tool.ts / hospital-tool.ts
  └─ LangChain tool 정의
  └─ 서울시 API 호출 + KST 영업상태 계산
  └─ JSON 포맷 변환

Step 4: generator.ts
  └─ LANGCHAIN_INTENTS에 추가
  └─ allTools 분기 추가

Step 5: prompt.ts
  └─ getIntentFocusHint에 pharmacy/hospital 추가

Step 6: PharmacyRenderer.tsx / HospitalRenderer.tsx
  └─ 카드 UI (card-preview.html v2 기반)
  └─ 목록 모드 / 단일 상세 모드

Step 7: ChatMessage.tsx
  └─ json:pharmacy, json:hospital 블록 파서
  └─ lazy import 추가

Step 8: 통합 테스트
  └─ "강남구 약국" → pharmacy_search 라우팅 확인
  └─ "타이레놀 부작용" → drug_info 유지 확인 (충돌 없음)
  └─ "강남구 병원" → hospital_search 라우팅 확인
  └─ "병원 드라마 추천" → general 유지 확인
```

---

## 9. 미결 사항

- [ ] 서울시 API 외 지역(경기, 부산 등) 처리 범위 결정 → 현재는 서울 한정
- [ ] 지도 링크 제공업체 결정: 카카오지도 vs 네이버지도 vs Google Maps
- [ ] 구 미지정 시 (예: "근처 약국") → 위치 권한 요청 vs "구를 알려주세요" 안내 중 선택
- [ ] 검색 결과 0건 시 UX — "해당 구에 검색 결과 없음" 카드 또는 LLM 일반 답변 폴백

---

## 10. 전체 Intent 현황 및 개선 로드맵

### 10-1. 현재 Intent 전체 구조

| Intent | 트리거 조건 | 데이터 소스 | Tool | 출력 |
|--------|-----------|-----------|------|------|
| `drug_id` | 이미지 첨부 + 의약품 키워드 | 대한약사회 DB (각인 검색) | `identifyPillTool` | `json:drug` → DrugRenderer |
| `drug_info` | 텍스트 약품명 질의 | 식약처 MFDS API → Google Search → LLM | `searchDrugInfoTool` | `json:drug` → DrugRenderer |
| `medical_qa` | 증상·질병·치료 질의 | **LLM 내부지식만** (⚠️ 개선 필요) | ❌ | 마크다운 텍스트 |
| `biology` | 생명과학 질의 | LLM | ❌ | `json:bio` → BioRenderer |
| `chemistry` | 화학·분자구조 질의 | LLM | ❌ | `json:smiles` → ChemicalRenderer |
| `physics` | 물리 시뮬레이션 질의 | LLM | ❌ | PhysicsRenderer |
| `astronomy` | 천문·별자리 질의 | LLM | ❌ | ConstellationRenderer |
| `data_viz` | 데이터·통계·차트 질의 | LLM | ❌ | `json:chart` → ChartRenderer |
| `general` | 나머지 모든 것 | LLM + Google Search Grounding | ❌ | 마크다운 텍스트 |
| `pharmacy_search` | 약국 위치 탐색 (신규) | 서울시 약국 API | `PharmacyTool` (신규) | `json:pharmacy` → PharmacyRenderer |
| `hospital_search` | 병원 위치 탐색 (신규) | 서울시 병원 API | `HospitalTool` (신규) | `json:hospital` → HospitalRenderer |

---

### 10-2. `medical_qa` Google Search 개선 계획

#### 배경
- 현재 `medical_qa`는 **LLM 내부 지식만** 사용 → hallucination 위험
- `drug_info`는 이미 Google Search Grounding이 적용되어 있음 (`searchDrugViaGoogleSearch()`)
- 동일 패턴 재사용으로 구현 비용 낮음

#### 개선 후 흐름
```
사용자: "두통 원인이 뭐야?" / "혈압약 먹어도 되나?"
    ↓ router → medical_qa
    ↓ generator → SDK path (LangChain 미사용 유지)
       → Google Search Grounding 활성화
         (config: { tools: [{ googleSearch: {} }] })
       → 검색 결과 기반 답변 생성
       → groundingChunks → 출처 칩 표시
    ↓ 마크다운 텍스트 + 출처 링크
```

#### 변경 파일
| 파일 | 변경 내용 |
|------|---------|
| `api/_lib/agent/nodes/generator.ts` | SDK path에서 `medical_qa`일 때 `googleSearch` 도구 활성화 |
| `api/_lib/agent/prompt.ts` | `medical_qa` hint — "Google Search 결과 기반 답변, 출처 명시" 강조 |

#### (선택) PubMed 추가 연결
- `medical_qa` + 임상/논문 관련 키워드 감지 시 PubMed Tool 병렬 호출
- Phase 2 `paper_search` 구현 후 연계 검토

#### 구현 우선순위
```
✅ Phase 1.5 완료 (2026-05-06) — medical_qa Google Search 개선
   - generator.ts: useGoogleSearch 강제 ON (이미지 없는 경우)
   - generator.ts: thinkingBudget: 3000 설정
   - generator.ts: token budget 8192 설정
   - prompt.ts: Google Search 기반 가이드라인 7개 명시
   - 테스트 결과: sources 12개, kdca.go.kr·amc.seoul.kr 등 신뢰도 높은 출처 칩 정상 표시
Phase 2       → paper_search + PubMed 연결 → medical_qa에도 선택적 적용
```

---

### 10-3. `drug_id` 상세 흐름

```
사용자: 알약 사진 첨부 + "이게 무슨 약이야?"
    ↓
router → drug_id (이미지 + 의약품 키워드 동시 감지)
    ↓
vision 노드 (전처리)
  → Gemini Vision으로 이미지 분석
  → 각인(앞/뒤)·색상·모양 추출
    ↓
generator → identifyPillTool 호출
  → 대한약사회(pharm.or.kr) DB 검색
  → 각인 변형 패턴 생성 (대/소문자, 특수문자 등)
  → 최대 5페이지 × 변형 수만큼 병렬 검색
  → 색상·모양 필터링으로 최종 후보 선정
    ↓
searchDrugInfoTool 추가 호출 (식약처 상세 데이터 보완)
    ↓
json:drug → DrugRenderer (이미지·각인·효능·용법 카드)
```

**주요 특징**:
- Vision이 "마크(로고)"로 읽은 각인 → `extractImprintViaVision()`으로 재판독
- 2~4자 각인은 중간 문자 삽입 변형 검색 (Vision 오인식 대응)
- 최종 매칭 실패 시 `searchWebTool`로 DuckDuckGo 검색 fallback
