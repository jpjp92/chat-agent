# 개발 노트 — 2026-04-13

---

# 이미지 분석 Latency & 세션 종료 버그 수정

## 증상

- 이미지 분석 요청 시 응답까지 ~17.5s 이상 소요 (단순 이미지도 동일)
- 가끔 분석 도중 세션이 종료되어 응답이 사라짐 (재시도 시 정상 동작)

## 테스트 로그 (성공 케이스 기준)

```
user message timestamp:  1776087270735
model timestamp:         1776087288259
→ 소요: 17,524ms ≈ 17.5초 (단순 이미지 분석 "이미지 분석" 요청)
```

---

## 원인 분석

### 실행 흐름

```
[useChatStream] 이미지 전송
  └─ /api/chat → Vercel Function 시작 (60s 카운트 시작)
       │
       ├─ [router.ts] gemini-2.5-flash-lite LLM 호출 (~1s)
       │              ← SSE 무음 구간 (클라이언트에 아무것도 전송 안 됨)
       │
       ├─ [generator.ts] sdkContents 빌드
       │    └─ 공개 URL 이미지 → fetch() → base64 변환 (~2~5s)
       │                         ← SSE 무음 구간 계속
       │
       ├─ [Gemini API] generateContentStream (~10~40s)
       │    └─ 이 시점부터 SSE 청크 전송 시작
       │
       └─ [chat.ts] 스트리밍 완료 후 Supabase 2회 순차 await ← 핵심
            await chat_messages.insert(...)   ← Vercel 60s 한계 소진
            await chat_sessions.update(...)
```

### 확인된 원인

#### ① Latency — Router 필수 LLM 호출 (`router.ts`)

모든 요청에 무조건 `gemini-2.5-flash-lite` LLM 분류 호출 (~1s).
이미지 + 의약품 키워드 없는 일반 이미지 분석은 어차피 `"general"` → Router를 건너뛸 수 있음.

#### ② Latency — 공개 URL 이미지 이중 왕복 (`generator.ts`)

3MB 이상 이미지: 클라이언트 → Supabase Storage 업로드 → 공개 URL.  
Generator가 이 URL을 서버에서 다시 `fetch()` → base64 변환 (~2~5s).  
Gemini SDK는 공개 URL을 `fileData`로 직접 지원하므로 재-fetch 불필요.

#### ③ 세션 종료 — Supabase 순차 await이 60s 한계 소진 (`chat.ts`)

스트리밍 완료 후 Supabase 쓰기 2회를 순차 `await`.  
스트리밍에 50s+ 소요 시 → Vercel 60s에 함수 강제 종료 → `res.end()` 미호출 → SSE abrupt close.  
클라이언트 `reader.read()` → `done: true` 에러 없이 탈출 → `modelResponse` 없으면 AI 메시지 미추가 → **응답 소실 = "세션 종료"처럼 보임.**

#### ④ 세션 종료 — SSE heartbeat 없음

Router + image re-fetch 구간 동안 클라이언트에 SSE 이벤트가 전혀 없음.  
일부 프록시/환경에서 무음 구간에 연결을 끊을 수 있음.

---

## 수정 결과

### 수정 1 — Supabase 쓰기 fire-and-forget (`api/chat.ts`) ✅

스트리밍 완료 즉시 `res.end()`, Supabase 쓰기는 `.then()` 비동기 처리.  
`finally { clearInterval(heartbeatInterval) }` 블록으로 heartbeat 타이머 정리.

```typescript
// 수정 전
await supabase.from('chat_messages').insert({ ... });
await supabase.from('chat_sessions').update({ ... });
res.end();

// 수정 후
res.end();  // 스트리밍 완료 즉시 응답 종료
supabase.from('chat_messages').insert({ ... }).then(({ error }) => { ... });
supabase.from('chat_sessions').update({ ... }).then(({ error }) => { ... });
```

### 수정 2 — SSE heartbeat (`api/chat.ts`) ✅

Router/Vision 실행 중 15s마다 `{ heartbeat: true }` 이벤트 전송.  
`geminiService.ts` SSE 파서에서 `if (data.heartbeat) continue`로 무시.

```typescript
const heartbeatInterval = setInterval(() => {
    res.write(`data: ${JSON.stringify({ heartbeat: true })}\n\n`);
}, 15000);
```

### 수정 3 — 공개 URL 이미지 fileData 직접 전달 (`generator.ts`) ✅

Supabase Storage 공개 URL은 `fileData: { fileUri: url, mimeType }`로 직접 전달.  
서버 side fetch → base64 변환 (~2~5s) 제거.  
mimeType은 URL 확장자로 추론 (기본 `image/jpeg`).

```typescript
// 수정 전
const fetchRes = await fetch(url);
const b64 = Buffer.from(await fetchRes.arrayBuffer()).toString('base64');
parts.push({ inlineData: { mimeType, data: b64 } });

// 수정 후
parts.push({ fileData: { fileUri: url, mimeType: mimeTypeHint } });
```

### 수정 4 — Vision Node URL 처리 동일 수정 (`vision.ts`) ✅

drug_id intent에서 Vision Node도 공개 URL 이미지를 재-fetch하던 문제 수정.  
`image_url` 타입으로 LangChain에 URL 직접 전달.

```typescript
// 수정 전: URL → fetch → base64
if (inlineData.startsWith("http")) {
    const res = await fetch(inlineData);
    inlineData = Buffer.from(await res.arrayBuffer()).toString('base64');
}

// 수정 후: URL 직접 전달
if (imageAtt.data.startsWith("http")) {
    imageUrlPart = { type: "image_url", image_url: { url: imageAtt.data } };
}
```

### 수정 5 — Router fast-path (`router.ts`) ✅

이미지 첨부 + 의약품 키워드 없음 → `"general"` 확정, Router LLM 호출 스킵 (~1s 단축).  
이미지 첨부 + 의약품 키워드 있음 → 기존대로 LLM 분류 진행 (`drug_id` 판단).

```typescript
const hasMedicalKeyword = medicalKeywords.some(k => textContent.includes(k)) || ...;
if (hasImage && !hasMedicalKeyword) {
    console.log('[LangGraph] Router fast-path: image without medical keyword → general');
    return { nextNode: "generator", intent: "general" };
}
```

---

## 예상 효과

| 항목 | 수정 전 | 수정 후 |
|------|---------|---------|
| 일반 이미지 분석 latency | ~17.5s | ~12~14s (Router ~1s + re-fetch ~2~5s 제거) |
| 세션 종료 | Supabase await → Vercel 60s 초과 시 발생 | `res.end()` 선행 → 해소 |
| SSE 무음 구간 | Router/Vision 실행 중 수십 초 무음 | 15s마다 heartbeat 전송 |
| drug_id 약품 이미지 | Vision + Generator 이중 URL fetch | URL 직접 전달로 각 1회 제거 |

---

# 전체 Intent 세션 종료 취약점 분석 — 2026-04-13

## 분석 범위

Supabase fire-and-forget 수정(v4.36) 이후에도 남아있는 세션 종료 위험 경로 전수 점검.

## 실행 경로 지도

```
모든 요청 → router.ts (LLM 분류)
  │
  ├─ general / medical_qa / biology / chemistry / physics / astronomy / data_viz
  │    └─ generator.ts [SDK path] → Gemini API만 사용
  │
  ├─ drug_id (이미지 + 의약품 키워드)
  │    └─ vision.ts → generator.ts [LangChain]
  │         ├─ identifyPillTool → pill-logic.ts → pharm.or.kr (30개 병렬, 6s timeout ✅)
  │         └─ (실패 시) searchWebTool → DuckDuckGo fetch ← 8s timeout ✅
  │
  └─ drug_info (텍스트 약품 검색)
       └─ generator.ts [LangChain]
            └─ searchDrugInfoTool → drug-info-tool.ts
                 ├─ getPharmOrKrDetailUrl() — 최대 4전략 순차 fetch ← 8s/iter timeout ✅
                 ├─ fetchMFDS() — MFDS API fetch                    ← 8s timeout ✅
                 ├─ extractImprintViaVision() — nedrug 이미지 fetch  ← 6s timeout ✅
                 └─ (실패 시) searchWebTool → DuckDuckGo fetch       ← 8s timeout ✅
```

## Intent별 위험도

| Intent | 외부 fetch 타임아웃 | 위험도 | 비고 |
|--------|-------------------|--------|------|
| general / medical_qa / biology / chemistry / physics / astronomy / data_viz | Gemini만 | ✅ 낮음 | v4.36 fix 적용 |
| data_viz | Gemini만 (flash-lite) | ✅ 낮음 | v4.36 fix 적용 |
| drug_id | `fetchPage()` 6s ✅ + DDG 8s ✅ | ✅ 낮음 | v4.40 fix 적용 |
| drug_info | 3곳 8s/6s/8s ✅ + DDG 8s ✅ | ✅ 낮음 | v4.40 fix 적용 |
| YouTube (트랜스크립트 없음, 장편) | Gemini 처리 지연 | 🟡 중간 | 30~50s 소요 가능 |

## drug_info 취약점 상세 (3곳)

### ① `getPharmOrKrDetailUrl()` — 순차 fetch 루프 (`drug-info-tool.ts:147`)

```typescript
for (const searchStr of uniqueStrategies) {  // 최대 4회 순차 실행
    const res = await fetch('https://www.pharm.or.kr/...', {
        method: 'POST', ...
        // AbortController 없음
    });
}
```

pharm.or.kr 응답 지연 시 전략 하나당 10~30s 대기.  
4전략 모두 실패 시 최대 40~120s → Vercel 60s 한계 돌파.  
`pill-logic.ts`의 `fetchPage()`는 6s timeout이 있지만, 이 함수에는 없음.

### ② `fetchMFDS()` — MFDS API fetch (`drug-info-tool.ts:272`)

```typescript
const res = await fetch(url, {
    headers: { 'User-Agent': 'curl/8.5.0', 'Referer': 'https://www.data.go.kr' }
    // AbortController 없음
});
```

MFDS API(`data.go.kr`) 응답 지연 시 무한 대기.  
Strategy 1 + Strategy 2 = 최대 2회 순차 호출.

### ③ `extractImprintViaVision()` — nedrug 이미지 download (`drug-info-tool.ts:20`)

```typescript
const imgRes = await fetch(imageUrl, {
    headers: { 'Referer': 'https://nedrug.mfds.go.kr/' }
    // AbortController 없음
});
```

DEV_260407에서 nedrug 서버가 이미지 요청을 차단하는 케이스 발견.  
차단/지연 시 무한 대기 → Vision LLM 호출 전에 hang.  
`Promise.all(items.map(...))` 내에서 실행되어 여러 item이 동시에 hang 가능.

### ④ `searchWebTool` — DuckDuckGo fetch (`tools.ts:13`)

```typescript
const res = await fetch("https://html.duckduckgo.com/html/", {
    method: "POST", ...
    // AbortController 없음
});
```

drug_id / drug_info 실패 시 fallback으로 호출.  
DDG가 느리거나 차단할 경우 hang.

## pill-logic.ts 안전 확인 ✅

`fetchPage()`는 `AbortController` + 6s timeout이 구현되어 있음.  
30개 병렬 요청 모두 6s 이내 종료 보장.

```typescript
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 6000);
const res = await fetch(SEARCH_URL, { ..., signal: controller.signal });
```

## 수정 완료 (2026-04-16)

| 순서 | 파일 | 대상 함수 | timeout | 상태 |
|------|------|-----------|---------|------|
| 1 | `drug-info-tool.ts` | `getPharmOrKrDetailUrl()` 루프 내 fetch | 8s AbortController | ✅ |
| 2 | `drug-info-tool.ts` | `fetchMFDS()` | 8s AbortController | ✅ |
| 3 | `drug-info-tool.ts` | `extractImprintViaVision()` 이미지 fetch | 6s AbortController | ✅ |
| 4 | `tools.ts` | `searchWebTool` DDG fetch | 8s AbortController | ✅ |
