# 개발 노트 — 2026-04-05

---

# Lighthouse 성능 개선 계획

## 현재 측정값 (2026-04-05, Desktop)
| 지표 | 값 | 점수 |
|------|----|------|
| FCP | 1.2s | 74 |
| SI | 1.2s | 92 |
| LCP | 1.6s | 77 |
| TBT | 170ms | 87 |
| CLS | 0.00 | 100 |
| **종합** | | **87** |

## 핵심 병목 분석

### 1. LCP Element render delay 3,320ms (최우선)
**원인**: `isAuthLoading = true` 동안 `<LoadingScreen>` 렌더 → 웰컴 메시지가 DOM에 없음 → auth + sessions API 왕복(~3,300ms) 이후에야 LCP 대상 요소 생성.

```
HTML(10ms) → JS → LoadingScreen 표시
    → /api/auth
    → /api/sessions (총 ~3,300ms)
    → isAuthLoading = false → 웰컴 메시지 DOM 생성 → LCP 측정
```

**목표**: LoadingScreen 제거 → 웰컴 메시지 즉시 렌더 → LCP ~200ms 기대

### 2. /api/sessions 크리티컬 패스 2,064ms
**원인**: `await loadUserSessions()` — sessions API 응답까지 화면 진입 블로킹.

### 3. 새 채팅 / 세션 전환 지연
- **새 채팅**: `createSession()` Supabase POST 응답 대기 (~500~1500ms)
- **세션 전환**: `fetchSessionMessages()` 응답 전까지 빈 화면

### 4. FontAwesome font-display (30ms)
- `index.html`에 `@font-face` override 이미 적용 중 — cdnjs 링크 자체 처리 필요

---

## 개선 방안

### A. LoadingScreen 제거 + 즉시 렌더
```typescript
// App.tsx — initAuth() 구조 변경
setIsAuthLoading(false);        // auth 완료 즉시 (sessions 기다리지 않음)
loadUserSessions(user.id);      // 백그라운드 로드
```
**필수 방어 장치 3가지** (auth 완료 전 액션 방지):
1. `!currentUser`이면 `ChatInput` disabled
2. auth 에러 시 Toast/에러 배너 표시
3. 사이드바 sessions 로딩 중 스켈레톤 UI

**예상 위험**:
| 시나리오 | 위험도 | 방어 |
|----------|--------|------|
| auth 완료 전 메시지 전송 | 🟡 중간 | ChatInput disabled |
| auth 에러 시 무반응 | 🔴 높음 | 에러 배너 |
| 세션 목록 깜빡임 | 🟡 중간 | 스켈레톤 |

### B. 새 채팅 Optimistic UI
```typescript
// 서버 응답 기다리지 않고 tempId로 즉시 화면 전환
const tempId = `temp_${Date.now()}`;
setSessions(prev => [{ id: tempId, title: '새 채팅', messages: [], createdAt: Date.now() }, ...prev]);
setCurrentSessionId(tempId);

// 백그라운드에서 실제 생성 후 tempId 교체
const { session } = await createSession(targetUserId);
setSessions(prev => prev.map(s => s.id === tempId ? { ...s, id: session.id, title: session.title } : s));
setCurrentSessionId(session.id);
```

### C. 세션 전환 스켈레톤
```tsx
// ChatArea — isLoadingMessages 동안 스켈레톤 버블 표시
{isLoadingMessages ? (
  <div className="space-y-4 py-4">
    {[1,2,3].map(i => <div key={i} className="h-12 rounded-2xl bg-slate-200/60 dark:bg-white/5 animate-pulse" />)}
  </div>
) : <ChatArea ... />}
```

## 구현 체크리스트

- [ ] `App.tsx` — `isAuthLoading` 제거 + 백그라운드 `loadUserSessions`
- [ ] `App.tsx` — auth 에러 시 에러 state + 배너 UI
- [ ] `components/ChatInput.tsx` — `!currentUser` 시 disabled 처리
- [ ] `components/ChatSidebar.tsx` — sessions 로딩 중 스켈레톤 (3개 pulse 아이템)
- [ ] `App.tsx` — `handleNewSession` Optimistic UI (tempId 패턴)
- [ ] `App.tsx` — `handleSelectSession` 중 `isLoadingMessages` state 추가
- [ ] `components/ChatArea.tsx` — 메시지 로딩 중 스켈레톤 표시

---

# 시각화 카드 전체화면 팝업 (Visualization Expand Modal)

## 기능 개요
시각화 카드(BioRenderer, ChemicalRenderer 등) 우하단에 expand 버튼을 추가하여
클릭 시 모달 팝업으로 해당 시각화를 전체화면 크기로 표시.

---

## 구현 방식

### 전반 구조
```
카드 우하단 expand 버튼 (fa-expand 아이콘)
    ↓ 클릭
App.tsx expandedViz state 업데이트 → { type, data }
    ↓
createPortal(document.body)로 VisualizationModal 마운트
    - backdrop blur + z-index 최상위
    - 동일 Renderer를 fullscreen 크기로 재렌더링
    - ESC 키 / X 버튼으로 닫기
```

### 상태 구조
```typescript
// App.tsx
const [expandedViz, setExpandedViz] = useState<{
    type: 'bio' | 'chemical' | 'physics' | 'constellation' | 'chart' | 'diagram' | 'drug';
    data: any;
} | null>(null);
```

### 주요 변경 파일
1. `App.tsx` — `expandedViz` state + `VisualizationModal` 컴포넌트 (Portal)
2. `components/ChatMessage.tsx` — 각 Renderer 래퍼에 expand 버튼 + `onExpand` 콜백
3. 각 Renderer 컴포넌트 — `isExpanded?: boolean` prop 수용 (크기 조정용)

### expand 버튼 위치
```tsx
// ChatMessage.tsx 내 각 Renderer 래퍼
<div className="relative group">
    <BioRenderer bioData={part.data} language={language} />
    <button
        onClick={() => onExpand?.({ type: 'bio', data: part.data })}
        className="absolute bottom-2 right-2 opacity-0 group-hover:opacity-100
                   bg-slate-700/80 text-white rounded-lg p-1.5 transition-opacity"
    >
        <i className="fa-solid fa-expand text-xs" />
    </button>
</div>
```

### VisualizationModal 구조 (Portal)
```tsx
// App.tsx — createPortal 사용
{expandedViz && createPortal(
    <div
        className="fixed inset-0 z-[9999] flex items-center justify-center p-4 sm:p-8"
        onClick={() => setExpandedViz(null)}
    >
        {/* Backdrop */}
        <div className="absolute inset-0 bg-black/60 backdrop-blur-md" />

        {/* Modal Content */}
        <div
            className="relative w-full max-w-5xl max-h-[90vh] overflow-auto
                       bg-white dark:bg-slate-900 rounded-3xl shadow-2xl
                       border border-slate-200 dark:border-white/10"
            onClick={(e) => e.stopPropagation()}
        >
            {/* Close Button */}
            <button
                onClick={() => setExpandedViz(null)}
                className="absolute top-4 right-4 z-10 w-8 h-8 rounded-full
                           bg-slate-100 dark:bg-white/10 flex items-center justify-center"
            >
                <i className="fa-solid fa-xmark text-sm" />
            </button>

            {/* Renderer (isExpanded=true) */}
            {renderExpandedContent(expandedViz)}
        </div>
    </div>,
    document.body
)}
```

---

## 렌더러별 고려사항

| 렌더러 | 팝업 적합성 | 특이사항 |
|--------|------------|---------|
| Bio 1D 서열 | ✅ 쉬움 | div 기반, 크기만 늘리면 됨 |
| Bio 3D PDB | ✅ 효과 큼 | NGL Stage가 stageRef에 마운트 → **새 인스턴스** 생성 필요 (기존 Stage와 독립) |
| Chemical (SMILES) | ✅ | canvas 기반, width/height prop 전달 |
| Physics | ⚠️ 주의 | Matter.js 엔진 두 개 동시 실행 → 모달 열 때 원본 pause 처리 권장 |
| Constellation | ✅ | canvas 크기 조정으로 처리 |
| Chart | ✅ | ApexCharts `width: '100%'` 설정 시 자동 채움 |
| Diagram | ✅ | canvas 2D, 크기 prop 전달 |
| Drug | ✅ | 정적 카드, 크기 확장만 |

### Physics pause 처리 방안
```typescript
// PhysicsRenderer.tsx
interface PhysicsRendererProps {
    // ...
    isPaused?: boolean; // 모달 팝업 시 원본 pause
}

useEffect(() => {
    if (isPaused) Matter.Runner.stop(runnerRef.current);
    else Matter.Runner.run(runnerRef.current, engineRef.current);
}, [isPaused]);
```

---

## 구현 체크리스트

- [ ] `App.tsx` — `expandedViz` state 추가
- [ ] `App.tsx` — `VisualizationModal` Portal 컴포넌트 구현
- [ ] `App.tsx` — ESC 키 `keydown` 이벤트로 닫기 처리
- [ ] `components/ChatMessage.tsx` — `onExpand` prop 타입 추가
- [ ] `components/ChatMessage.tsx` — 각 Renderer 래퍼에 group + expand 버튼 추가 (7종)
- [ ] `components/BioRenderer.tsx` — `isExpanded` prop 수용 (3D 캔버스 height 조정)
- [ ] `components/PhysicsRenderer.tsx` — `isPaused` prop 수용 + Runner 제어
- [ ] 테스트: ESC 키 닫기 동작 확인
- [ ] 테스트: 모바일 탭 expand 버튼 접근성 확인
- [ ] 테스트: Physics 원본 pause / 모달 resume 확인
- [ ] 테스트: Bio 3D PDB 새 NGL Stage 독립 렌더링 확인

---

# 세션 Race Condition 버그 수정

## 버그 증상
새로고침 후 웰컴 화면에서 메시지를 입력하면, 새 채팅이 아닌 **기존 마지막 세션에 메시지가 추가**되는 문제.

## 원인
`loadUserSessions`에서 `handleSelectSession(mappedSessions[0].id)` 자동호출 → `currentSessionId`가 기존 세션 ID로 설정된 상태에서 웰컴 화면 표시 → 사용자가 입력 시 `handleSendMessage`가 기존 세션 ID로 메시지 추가.

## 수정 내용

### 1. `loadUserSessions` — 자동 세션 선택 제거 (`App.tsx`)
```typescript
// 변경 전
setSessions(mappedSessions);
if (mappedSessions.length > 0) handleSelectSession(mappedSessions[0].id);

// 변경 후
setSessions(mappedSessions);
// 자동 선택하지 않음 — 웰컴 화면에서 첫 메시지 전송 시 새 세션 생성
```

### 2. `handleSendMessage` — null 세션 시 새 세션 자동 생성 (`App.tsx`)
```typescript
// 변경 전
if (!currentSessionId || (!content.trim() && ...)) return;

// 변경 후
if (!content.trim() && attachments.length === 0) return;

let activeSessionId = currentSessionId;
if (!activeSessionId) {
  const { session, error } = await createSession(targetUserId);
  // 신규 세션 생성 후 setSessions + setCurrentSessionId
  activeSessionId = session.id;
}
```

### 3. 함수 내부 `currentSessionId` → `activeSessionId` 교체
`handleSendMessage` 내 `currentSessionId` 참조 9개를 `activeSessionId`로 교체 — 새로 생성된 세션 ID 기준으로 모든 state 업데이트 수행.

## 결과
- 새로고침 → 웰컴 화면 → 입력 → **새 세션 자동 생성** 후 대화 시작 ✅
- 기존 세션 선택 후 대화 시 동작 변경 없음 ✅

---

# UI 폴리시 2차 (2026-04-05)

## 채팅 소스마스크
- **펼쳐진 너비**: `280px/300px` → `260px/272px` (sm 기준)

## 헤더 프로필 모달
- **너비**: `max-w-sm(384px)` → `max-w-xs(320px)`
- **모서리**: `rounded-[32px]` → `rounded-2xl`
- **패딩**: `p-6 space-y-6` → `p-4 space-y-4`
- **아바타**: `w-24 h-24` → `w-20 h-20`
- **버튼**: `py-3.5 rounded-2xl` → `py-2.5 rounded-xl`
- **모달 위치**: `pb-20 sm:pb-48`(고정픽셀) → `pb-[22dvh]`(dynamic viewport height) — 기기별 편차 최소화

## 삭제 확인 다이얼로그 (Dialog.tsx)
- **너비**: `max-w-sm` → `max-w-xs`
- **모서리**: `rounded-[28px]` → `rounded-2xl`
- **패딩**: `p-8 mt-8` → `p-5 mt-5`
- **아이콘**: `w-14 h-14 rounded-2xl` → `w-12 h-12 rounded-xl`
- **제목**: `text-xl` → `text-base`
- **메시지**: `text-sm` → `text-xs`, `whitespace-pre-line` 추가
- **버튼**: `py-4 rounded-2xl` → `py-2.5 rounded-xl`
- **안내 멘트**: 한 줄 단락 → 두 줄 분리 (`\n` 삽입, 4개 언어 모두)

## ChatArea 상단 간격
- `ChatArea` 컨테이너에 `pt-4` 추가 — 첫 메시지가 헤더에 너무 붙는 문제 해결

---

# 스트리밍 실시간 전송 버그 수정 (2026-04-05)

## 버그 증상
질문 전송 시 응답이 스트리밍되지 않고 **완성된 전체 텍스트가 한 번에 나타나는** 문제. 첫 전송 시 웰컴 화면으로 잠깐 되돌아가는 현상도 동반.

## 원인
SDK path(`medical_qa` / `biology` 등)의 `generator.ts`에서 `genai.models.generateContent()` (비스트리밍) 사용 중 — 응답 완성 후 단일 이벤트로 전송.

## 수정 내용

### `api/_lib/agent/nodes/generator.ts`
```typescript
// 변경 전
const result = await genai.models.generateContent({ ... });
responseText = result.text ?? '';

// 변경 후
const sdkStream = await genai.models.generateContentStream({ ... });
for await (const chunk of sdkStream) {
    const chunkText = chunk.text ?? "";
    if (chunkText) {
        responseText += sanitized;
        if (sendEvent) sendEvent({ text: sanitized }); // 청크 즉시 전송
    }
}
```
- `createGeneratorNode(systemInstructionBase, isYoutubeRequest, sendEvent?)` 파라미터 추가
- 마지막 청크에서 `groundingMetadata` 수집 후 `sendEvent({ groundingSources })` 전송

### `api/_lib/agent/graph.ts`
- `compileAgentGraph(systemInstruction, isYoutubeRequest, sendEvent?)` 파라미터 추가
- `createGeneratorNode`에 `sendEvent` 전달

### `api/chat.ts` — `trackingEvent` 패턴
```typescript
let fullAiResponse = '';
const trackingEvent = (data: any) => {
    if (data.text) fullAiResponse += data.text; // Supabase 저장용 자동 누적
    sendEvent(data);
};
const graph = compileAgentGraph(systemInstruction, isYoutubeRequest, trackingEvent);
```
- `on_chain_end(generator)`: fallback 전용 — SDK/LangChain 스트리밍으로 이미 전송된 경우 중복 방지

## 결과
- 응답이 청크 단위로 실시간 타이핑 효과로 표시됨 ✅
- LangChain path(`drug_id` / `drug_info`)는 기존 `on_chat_model_stream` 유지 ✅

---

# 웰컴화면 질의 미표시 버그 수정 (2026-04-05)

## 버그 증상
새 세션 생성 후 **사용자 질의 버블이 화면에 나타나지 않음**. 사이드바에서 해당 채팅을 다시 클릭해야 히스토리가 보임.

## 원인
`handleSendMessage`에서 두 번의 분리된 `setSessions` 호출 → React 배치 최적화에서 첫 번째 호출(userMessage 포함)이 두 번째 호출(빈 messages)에 덮어씌워짐.

## 수정 내용 (`App.tsx`)
```typescript
// 1. userMessage 먼저 생성
const userMessage: Message = { id, role: Role.USER, content, ... };

// 2. 새 세션 생성 경로 — userMessage를 초기 messages에 직접 포함
const newSess: ChatSession = {
    id: session.id,
    title: session.title,
    messages: [userMessage], // ← 직접 포함
    ...
};
setSessions(prev => [newSess, ...prev]); // 단일 setSessions
```

## 결과
- 새 세션 생성 즉시 사용자 질의 버블이 화면에 표시됨 ✅
- AI 응답 스트리밍도 동일 세션에 연속 표시됨 ✅

---

# 사이드바 ⋯ 드롭다운 메뉴 (2026-04-05)

## 문제
긴 제목을 가진 세션 항목에서 편집/삭제 버튼(`absolute right-2`)이 `truncate ...` 생략 기호 위에 겹쳐 UX 저해.

## 해결 방안 (방안 B 적용)
기존 편집/삭제 버튼 2개 → `⋯` 단일 아이콘 버튼 + 드롭다운 메뉴 방식으로 교체.

## 구현 (`components/ChatSidebar.tsx`)

### 추가 state / ref
```typescript
const [openMenuId, setOpenMenuId] = useState<string | null>(null);
const menuRef = useRef<HTMLDivElement>(null);

useEffect(() => {
    const handleClickOutside = (e: MouseEvent) => {
        if (menuRef.current && !menuRef.current.contains(e.target as Node))
            setOpenMenuId(null);
    };
    if (openMenuId) document.addEventListener('mousedown', handleClickOutside);
    return () => document.removeEventListener('mousedown', handleClickOutside);
}, [openMenuId]);
```

### 세션 아이템 구조
- 제목 `text-[13px] tracking-tight pr-5` — ⋯ 버튼 공간 확보
- `overflow-hidden` 제거 — 드롭다운이 아이템 밖으로 표시되도록
- hover 시 ⋯ 버튼 노출 → 클릭 시 `openMenuId` 토글
- 드롭다운: `w-24 rounded-xl shadow-lg`, 편집(fa-pen) + 삭제(fa-trash-can, text-red-500)

### 다국어 처리
i18n 객체에 `edit` / `delete` 키 추가:

| 언어 | edit | delete |
|------|------|--------|
| ko | 편집 | 삭제 |
| en | Edit | Delete |
| es | Editar | Eliminar |
| fr | Modifier | Supprimer |
