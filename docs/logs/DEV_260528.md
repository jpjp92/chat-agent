# DEV_260528 — 2.5 Flash URL 패칭 / 빈 응답 처리 / 렌더링 버그 수정

> 작성일: 2026-05-28

---

## 1. 현상

텍스트 중간에 URL이 포함된 메시지를 입력했을 때:

- **3.5 Flash**: URL을 텍스트 참조로 처리 → 정상 분석
- **2.5 Flash**: URL을 패칭/검색 대상으로 인식 → 엉뚱한 결과 또는 불필요한 웹 조회 발생

---

## 2. 원인 분석

### 원인 1 — `historyHasUrl` 의 blind spot (`generator.ts`)

```typescript
// 기존 코드
const historyHasUrl = state.messages.slice(0, -1)  // ← 현재 메시지 제외!
    .filter((m: any) => m._getType() === 'human')
    .some((m: any) => /https?:\/\/\S+/.test(text));
if (historyHasUrl) useGoogleSearch = false;
```

`slice(0, -1)`로 히스토리만 스캔하기 때문에 **현재 턴(첫 번째 메시지)**에 URL이 있어도 `historyHasUrl = false` → Google Search가 비활성화되지 않음.

### 원인 2 — Google Search grounding 가용성 차이

| 모델 | Google Search | 경로 |
|------|--------------|------|
| 2.5 Flash | 사용 가능 (무료 티어) | Single-pass: 원본 메시지 + Google Search 직접 노출 |
| 3.5 Flash | 무료 티어 불가 (`needsSearchFallback = true`) | Two-track: Stage2(3.5)는 Stage1 요약 텍스트만 수신, 원본 URL 미노출 |

### 원인 3 — 모델 특성

2.5 Flash는 thinking 기반 추론 모델 특성상, Google Search가 활성화된 상태에서 URL을 발견하면 "이 URL의 내용을 검색해서 답해야 한다"고 추론하는 경향이 강함.

---

## 3. 수정 — `generator.ts`

`historyHasUrl` 체크에 현재 메시지 URL 감지(`currentMsgHasNonYtUrl`)를 추가.

YouTube URL은 영상 분석 기능에 영향을 주지 않도록 제외.

```typescript
// 현재 메시지도 URL 체크 (slice(0,-1)이라 현재 턴 누락 보완)
const currentMsgHasNonYtUrl = (() => {
    const lastMsg = state.messages[state.messages.length - 1];
    if (!lastMsg || lastMsg._getType() !== 'human') return false;
    const text = Array.isArray(lastMsg.content)
        ? (lastMsg.content as any[]).filter((p: any) => p.type === 'text').map((p: any) => p.text).join('')
        : String(lastMsg.content);
    return /https?:\/\/\S+/.test(text) && !/(?:youtube\.com|youtu\.be)/.test(text);
})();
if (historyHasUrl || currentMsgHasNonYtUrl) useGoogleSearch = false;
```

**수정 파일**: `server/agent/nodes/generator.ts`

**효과**:
- 2.5 Flash도 텍스트 내 URL을 텍스트 그대로 분석 (패칭 시도 없음)
- YouTube URL은 제외 → 영상 native 분석 동작 유지
- 3.5 Flash two-track 경로는 기존과 동일하게 동작

---

## 4. 2.5 Flash 멀티턴 "제공된 정보에 따르면" 반복 수정

### 현상

멀티턴 대화에서 2.5 Flash가 응답을 "제공된 정보에 따르면", "제시된 정보를 바탕으로" 등의 문구로 시작하는 경향 발견. 웹보다 모바일에서 더 빈번하게 관찰됨.

- **3.5 Flash**: 정상 (해당 문구 없음)
- **2.5 Flash**: 멀티턴 진행 시 반복적으로 발생

### 원인 분석

`generator.ts`가 시스템 프롬프트에 `[PROVIDED_SOURCE_TEXT]` 태그를 주입하면, 2.5 Flash가 해당 컨텍스트를 읽고 자연스럽게 "제공된 정보에 따르면"으로 응답을 시작하는 모델 특성이 원인.

| 경로 | 금지 규칙 존재 여부 |
|------|-------------------|
| 3.5 Flash two-track Stage2 (`synthesisInstruction`) | ✅ 있음 (line 426) |
| 2.5 Flash 직접 SDK 경로 (`getSystemInstruction`) | ❌ 없음 |

모바일에서 더 빈번한 이유: 캐시 문제가 아닌, 모바일 세션에서 멀티턴 대화가 더 길게 이어지는 사용 패턴 때문.

### 수정 — `server/agent/prompt.ts`

`getSystemInstruction`의 `[FORMATTING & QUALITY]` 섹션에 서두 문구 금지 규칙 추가.

```typescript
// 추가된 규칙 (한/영/스/불 + 변형 포함)
- NEVER start a response with source attribution phrases such as
  "제공된 정보에 따르면", "제시된 정보를 바탕으로", "제공된 정보에 의하면",
  "Based on the provided information", "Based on the information provided",
  "According to the provided information",
  "Según la información proporcionada", "D'après les informations fournies",
  or any equivalent. Start directly with the answer content.
```

**수정 파일**: `server/agent/prompt.ts`

**효과**:
- 2.5 Flash 멀티턴에서 "제공된 정보에 따르면" 문구 제거
- 3.5 Flash에도 동일하게 적용 (기존 two-track 금지 규칙과 이중 방어)
- 4개 언어(한/영/스/불) 및 변형 패턴 모두 커버

---

## 5. 멀티턴 서두 문구 금지 규칙 개선 — "정보" → "내용" 변형 대응

### 현상

4번 수정 이후에도 "제시된 **내용**을 바탕으로 한 X는 다음과 같습니다" 형태가 잔존. `정보` 대신 `내용` 단어 변형으로 기존 차단 목록을 우회.

### 수정

기존 규칙이 **완전 금지**였던 것을 **boilerplate opener로 사용 시만 금지**로 완화 + 변형 패턴 추가.

| 파일 | 변경 내용 |
|------|----------|
| `server/agent/prompt.ts` | "제시된 내용을 바탕으로", "주어진 정보/내용을 바탕으로" 등 추가. 문맥상 의미 있는 경우는 허용, 형식적 opener로 사용 시만 금지 |
| `server/agent/nodes/generator.ts` `synthesisInstruction` | 동일 변형 패턴 추가 + "any paraphrase referencing provided/given/presented information" 포괄 금지 |

**핵심 방향**: 구문 자체 전면 금지 → 매 멀티턴마다 rote opener로 반복 사용 금지, 문맥상 자연스러운 경우는 허용.

---

## 6. 빈 응답(empty response) 처리 강화

### 현상 (에러 로그)

```
[LangGraph] SDK call failed: undefined SDK returned empty response text
[LangGraph] SDK path failed for intent: general — falling back to LangChain (no streaming)
[browser] Chat streaming failed Error: LLM returned empty response.
```

`gemini-3.5-flash` 멀티턴(`thinkingLevel: "medium"`)에서 SDK가 빈 텍스트 반환 → LangChain fallback도 빈 응답 → 클라이언트 에러.

### 원인 분석

`finishReason` / `safetyRatings` 로그가 없어 원인 진단 불가. 가능한 원인:
1. `thinkingLevel: "medium"` → thinking 토큰이 모든 출력 토큰을 소진, 텍스트 파트 없음
2. `finishReason === 'SAFETY'` → 콘텐츠 안전 필터 차단

### 수정 — `server/agent/nodes/generator.ts`

**① thinking exhaustion 자동 재시도**: 빈 응답 + 3.5 모델 + non-minimal thinkingLevel이면 `"minimal"` 로 재시도 후 그래도 빈 응답 시 LangChain fallback.

```typescript
if (!responseText && is3xModel && thinkingConfig && thinkingConfig.thinkingLevel !== 'minimal') {
    console.warn('[LangGraph] Empty response — retrying with minimal thinking');
    // thinkingLevel: 'minimal' 로 재호출
}
```

**② finishReason / safetyRatings 로깅**: 빈 응답 시 원인 진단 로그 추가.

```
[LangGraph] Empty response - finishReason: SAFETY | safetyRatings: [...]
```

**③ SAFETY 차단 즉시 propagate**: `safetyBlock` 플래그 에러 생성, 키 재시도 없이 즉시 throw.

### 수정 — `app/api/chat/route.ts`

`CHAT_ERRORS`에 `safety` 타입 추가 (4개 언어), 에러 분류 로직에 `safetyBlock` 체크 우선 적용.

```typescript
safety: { ko: '안전 정책에 의해 응답이 차단되었습니다. 질문을 다르게 표현해보세요.', ... }
```

---

## 7. 마크다운 렌더링 버그 2종 수정 — `ChatMessage.tsx`

### 버그 1 — `<br>` 태그 그대로 노출

**원인**: `rehypeRaw` 미설치 → 마크다운 테이블 셀 내 `<br>` HTML이 텍스트 노드로 처리됨.

**수정**: 텍스트 전처리 3곳(`textPart`, `processedVisible`, `processedRemaining`)에서 `<br>` → ` · ` 변환.

```typescript
textPart = textPart.replace(/<br\s*\/?>/gi, ' · ');
```

### 버그 2 — `**` 볼드 마커 노출 (숫자 뒤 한글 패턴)

**원인**: 기존 lookahead `(?=[가-힣A-Za-zÀ-ÿ])`가 숫자(`0-9`)를 포함하지 않아 `**25% 수준의 GPU 자원**만으로` 같은 패턴 미처리.

**수정**: lookahead에 `0-9` 추가.

```typescript
// 변경 전
textPart.replace(/\*\*([^*\n]+?)\*\*(?=[가-힣A-Za-zÀ-ÿ])/g, '**$1** ')
// 변경 후
textPart.replace(/\*\*([^*\n]+?)\*\*(?=[가-힣A-Za-zÀ-ÿ0-9])/g, '**$1** ')
```

---

## 8. 표 셀 간결성 규칙 강화 — `prompt.ts`

### 현상

표 내용 컬럼이 `~입니다 / ~합니다` 형태의 완전한 서술 문장으로 생성되어 스캔 가독성 저하.

### 수정

`[CELL CONTENT LIMIT]` 규칙 강화:

- **≤12 words** 상한 (기존 ≤25 words)
- `~입니다 / ~합니다 / ~있습니다 / ~됩니다` 등 서술 어미 금지
- **키워드·구절 스타일** 강제 (fragment/note style)
- `→ ↑ ↓ ·` 압축 표기 권장
- 12단어 초과 내용은 표 아래 산문으로 분리

**기대 출력 변화**:

| 구분 | 이전 | 이후 |
|------|------|------|
| 핵심 메커니즘 | 이미지에 미세한 독성 패턴을 주입해 AI가 전혀 다른 객체로 인식하도록 왜곡을 유도합니다. | 독성 픽셀 주입 → AI 오인식 유도 |
| 강도 | 독성 주입 강도를 조절하며, 값이 높을수록 원본과의 시각적 차이가 커질 수 있습니다. | 높을수록 원본 시각 차이↑ |

---

## 9. DB 쿼리 최적화 — 입출력 효율화

> 검토일: 2026-05-28 / 구현 완료: 2026-05-28

### 배경

현재 DB 상태 검토 결과, 아래 5가지 비효율 패턴이 확인됨:
- `count: 'exact'` → 매 세션 목록 요청마다 `COUNT(*)` 실행
- `select('*')` → 사이드바에 필요 없는 컬럼까지 전부 조회
- POST/PATCH 후 `.select()` → 전체 컬럼 반환
- 메시지 저장 시 `chat_sessions.update({ updated_at })` 별도 write
- DB 트리거 없음 → 앱 코드에서 `updated_at` 수동 관리

---

### 변경 1 — `sessions/route.ts` GET (세션 목록)

**현재:**
```ts
.select('*', { count: 'exact' })
.eq('user_id', user_id)
.order('updated_at', { ascending: false })
.range(offset, offset + limit - 1)
// → return { sessions, total: count ?? 0 }
```

**변경:**
```ts
.select('id, title, created_at, updated_at')
.eq('user_id', user_id)
.order('updated_at', { ascending: false })
.range(offset, offset + limit - 1)
// → return { sessions, hasMore: (sessions?.length ?? 0) === limit }
```

- `count: 'exact'` 제거 → `COUNT(*)` 쿼리 없음
- `total` → `hasMore: boolean` 패턴으로 교체
- `user_id` 컬럼 제외 (사이드바에서 미사용)

---

### 변경 2 — `sessions/route.ts` GET (메시지 조회)

**현재:**
```ts
.select('*')
.eq('session_id', session_id)
.order('created_at', { ascending: true })
```

**변경:**
```ts
.select('id, role, content, attachment_url, grounding_sources, created_at')
.eq('session_id', session_id)
.order('created_at', { ascending: true })
```

- `session_id` 컬럼 제외 (`mapDbMessage`에서 미사용)
- 나머지 컬럼은 모두 렌더링에 필요 → `grounding_sources`(jsonb) 포함 유지

---

### 변경 3 — `sessions/route.ts` POST / PATCH 컬럼 명시

**현재:**
```ts
// POST
.select()  // → select('*')

// PATCH
.select()  // → select('*')
```

**변경:**
```ts
// POST
.select('id, title, created_at, updated_at')

// PATCH
.select('id, title, updated_at')
```

---

### 변경 4 — `useChatSessions.ts` hasMore 패턴 전환

**현재:**
```ts
const { sessions: dbSessions, total } = await fetchSessions(resolvedUserId, 0, SESSION_PAGE_SIZE);
setHasMore((total ?? 0) > SESSION_PAGE_SIZE);
// loadMore:
setHasMore(nextOffset < (total ?? 0));
```

**변경:**
```ts
const { sessions: dbSessions, hasMore } = await fetchSessions(resolvedUserId, 0, SESSION_PAGE_SIZE);
setHasMore(hasMore ?? false);
// loadMore:
setHasMore(hasMore ?? false);
```

- `total` 의존 제거 → API 응답 `hasMore: boolean` 직접 사용
- `SESSION_PAGE_SIZE`와 정확히 일치하는 경우(예: 30개 딱 맞으면) 빈 페이지 1번 더 로드 가능 — 허용 가능한 엣지케이스

---

### 변경 5 — DB 트리거 + `chat/route.ts` sessions write 제거

#### 5-1. Supabase Dashboard에서 트리거 생성 (선행 필수)

```sql
-- Function: chat_messages INSERT 시 부모 세션 updated_at 자동 갱신
CREATE OR REPLACE FUNCTION update_session_timestamp()
RETURNS TRIGGER AS $$
BEGIN
  UPDATE chat_sessions SET updated_at = now() WHERE id = NEW.session_id;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger: AFTER INSERT on chat_messages
CREATE TRIGGER trg_message_updates_session
AFTER INSERT ON chat_messages
FOR EACH ROW EXECUTE FUNCTION update_session_timestamp();
```

#### 5-2. `chat/route.ts` — sessions.update() 제거

트리거 생성 확인 후 코드 수정. 2곳 동일하게 적용:

**현재 (line 155-156, 246-247):**
```ts
await Promise.all([
  supabase.from('chat_messages').insert({ session_id, role: 'assistant', content: fullAiResponse, grounding_sources: ... }),
  supabase.from('chat_sessions').update({ updated_at: new Date().toISOString() }).eq('id', session_id)
]);
```

**변경:**
```ts
await supabase.from('chat_messages').insert({
  session_id, role: 'assistant', content: fullAiResponse,
  grounding_sources: allSources.length > 0 ? allSources : null
});
// updated_at은 트리거가 자동 처리
```

`sessions/route.ts` PATCH도 동일하게 `updated_at` 수동 세팅 제거:
```ts
// 현재
.update({ title, updated_at: new Date().toISOString() })
// 변경
.update({ title })
```

---

### 구현 순서

| 순서 | 작업 | 위치 |
|---|---|---|
| 1 | Supabase Dashboard — 트리거 함수 + 트리거 생성 | DB |
| 2 | `sessions/route.ts` — 변경 1~3 (컬럼 명시 + hasMore 패턴) | 서버 |
| 3 | `useChatSessions.ts` — 변경 4 (hasMore 패턴) | 프론트 |
| 4 | 트리거 동작 확인 후 `chat/route.ts` + `sessions/route.ts` PATCH — sessions write 제거 | 서버 |

> 트리거 생성 전에 4번을 먼저 적용하면 `updated_at`이 갱신되지 않아 세션 목록 정렬이 깨짐. 반드시 순서 준수.

---

## 10. LCP 개선 — `isAuthLoading` 블로킹 해소

> 검토일: 2026-05-28 / 구현 완료: 2026-05-28

### 현재 문제

```
앱 접속
  → isAuthLoading = true
  → <LoadingScreen> 표시 (앱 전체 차단)
  → initAuth() 실행
      └─ localStorage에 저장된 유저 없음 → POST /api/auth (~수백ms~수초)
  → isAuthLoading = false → 비로소 앱 렌더링
```

**영향 대상**: 첫 방문자 또는 localStorage 캐시가 없는 사용자.  
저장된 유저가 있으면 `JSON.parse`만으로 즉시 해소되므로 재방문자는 해당 없음.

현재 구현 (`App.tsx:293-296`):
```tsx
if (isAuthLoading) {
  return <LoadingScreen message={t.preparingSession} />;
}
```

### 목표

앱 UI를 즉시 렌더하되, 인증 완료 전까지 입력만 비활성화. 로딩은 백그라운드에서 진행.

```
앱 접속
  → UI 즉시 렌더 (사이드바 skeleton + ChatInput disabled)
  → 백그라운드: initAuth() 실행
  → 완료 → ChatInput 활성화 + 세션 목록 로드
```

### 변경 내용

**① `App.tsx` — isAuthLoading 전체 차단 제거**

```tsx
// 제거
if (isAuthLoading) {
  return <LoadingScreen message={t.preparingSession} />;
}

// 유지 (currentUser 확정 실패 시 에러 화면)
if (!currentUser && !isAuthLoading) {
  return <LoadingScreen message={errMsg}><RetryButton /></LoadingScreen>;
}
```

**② `ChatInput` — 인증 완료 전 disabled 처리**

```tsx
// ChatInput에 isAuthLoading prop 추가
<ChatInput
  disabled={isAuthLoading || isLimitReached}
  placeholder={isAuthLoading ? t.preparingSession : t.inputPlaceholder}
  ...
/>
```

**③ 사이드바 — 이미 skeleton 구현됨 (`ChatSidebar.tsx:230`)**

`isLoadingSessions && sessions.length === 0` 조건에서 shimmer skeleton 이미 표시 중. 추가 작업 불필요.

**④ 메시지 영역 — 이미 spinner 구현됨 (`App.tsx:374`)**

`isLoadingMessages` 시 spinner overlay + blur 처리 이미 구현됨. 추가 작업 불필요.

---

## 11. DB 최적화 + LCP 개선 통합 실행 계획

### 체감 효과 흐름

```
[현재]
접속 → LoadingScreen 전체 차단 (~수백ms~수초) → 세션 목록 + COUNT(*) → 사용 가능

[개선 후]
접속 → UI 즉시 표시 (skeleton) → 백그라운드 인증 → 세션 로드 (COUNT(*) 없음) → 사용 가능
```

### Before / After 비교

| 항목 | Before | After |
|---|---|---|
| 첫 화면 표시 | `isAuthLoading` 동안 차단 | 즉시 skeleton UI 표시 |
| 인증 대기 중 | 흰 로딩 화면 | 사이드바 shimmer + input disabled |
| 세션 목록 쿼리 | `SELECT * + COUNT(*)` | `SELECT id,title,created_at,updated_at` |
| 페이지네이션 판단 | `total > PAGE_SIZE` | `hasMore: boolean` (length 비교) |
| 메시지 저장 write 수 | 3회 (user + assistant + session) | 2회 (user + assistant, session은 트리거) |
| `updated_at` 관리 | 앱 코드 수동 | DB 트리거 자동 |

### 실행 순서

| 순서 | 작업 | 파일 | 비고 |
|---|---|---|---|
| 1 | Supabase — 트리거 생성 | DB Dashboard | 코드 변경 전 선행 필수 |
| 2 | `sessions/route.ts` — 쿼리 최적화 (변경 1~3) | 서버 | `hasMore` 패턴 포함 |
| 3 | `useChatSessions.ts` — `hasMore` 패턴 전환 (변경 4) | 프론트 | |
| 4 | `chat/route.ts` + `sessions/route.ts` PATCH — sessions write 제거 (변경 5) | 서버 | 트리거 확인 후 |
| 5 | `App.tsx` — `isAuthLoading` 전체 차단 제거 | 프론트 | |
| 6 | `ChatInput` — `isAuthLoading` disabled 처리 | 프론트 | |
| 7 | 통합 검증 — 첫 방문 / 재방문 / 세션 전환 / 메시지 전송 시나리오 | — | ✅ 완료 |

> 전체 Steps 1~7 완료 확인 (2026-05-28). 메시지 전송 후 세션 `updated_at` 정렬 트리거 동작 검증 완료.

---

## 12. 볼드 마커 `**` 노출 — 다중 bold span 전처리 회귀 수정

> 구현 완료: 2026-05-28

### 현상

URL 요약/비교 설명처럼 한 문장 또는 한 bullet 안에 `**...**` bold span이 여러 번 등장할 때, 일부 `**` 기호가 렌더링되지 않고 그대로 노출.

대표 재현:

```markdown
* **Codex의 확장 레이어**: Codex를 감싸서 **프롬프트 관리, 계획 수립, 런타임 환경**을 보완합니다.
```

이전 보정 결과:

```markdown
* **Codex의 확장 레이어 **: Codex를 감싸서 ** 프롬프트 관리, 계획 수립, 런타임 환경**을 보완합니다.
```

첫 번째 closing `**`를 두 번째 bold span의 opening처럼 오인해 bold 내부 앞뒤에 공백이 삽입되고, ReactMarkdown이 일부 마커를 raw text로 렌더링.

### 원인 분석

1차 가설은 `K**text**`처럼 여는 `**` 앞에 한글/영숫자가 바로 붙는 패턴이 CommonMark flanking 규칙상 opener로 인식되지 않는다는 것이었음.

하지만 실제 사용자 샘플의 핵심 실패는 **여는 delimiter 누락**이 아니라, 전처리 정규식이 이전 bold span의 **닫는 delimiter**를 다음 span의 여는 delimiter로 잘못 매칭한 것.

문제 정규식:

```typescript
/([가-힣A-Za-zÀ-ÿ0-9])\*\*([^\s*\n][^*\n]*?)\*\*/g
```

이 패턴은 "한글/영숫자 바로 뒤의 opening `**`"만 잡는 의도였으나, `...레이어**: ... **프롬프트...**` 구조에서 `레이어**: ... **` 구간을 하나의 span처럼 잡아 closing marker를 파괴.

### 수정 — `components/ChatMessage.tsx`

3곳(`textPart`, `processedVisible`, `processedRemaining`) 동일하게 적용.

1. 위험한 opening-side 보정 제거
2. closing `**` 뒤에 한글/영숫자가 붙는 케이스만 보정
3. 단, 보정 대상 bold span의 opening `**`가 줄 시작/공백/문장부호 뒤에서 시작할 때만 매칭해 이전 closing marker 오인을 방지

```typescript
// 따옴표 뒤 closing ** + 한국어/영숫자
textPart = textPart.replace(/(["""'''])\*\*(?=[가-힣A-Za-zÀ-ÿ0-9])/g, '$1‌** ');

// plausible opener 위치에서 시작한 **span**만 closing 보정
textPart = textPart.replace(
  /(^|[\s([{"'“‘>:*-])\*\*([^*\n]+?)\*\*(?=[가-힣A-Za-zÀ-ÿ0-9])/g,
  '$1**$2** '
);
```

### 회귀 검증 — `scripts/test-markdown-bold-regression.mjs`

실제 `react-markdown + remark-gfm` 렌더링을 사용해 HTML에 raw `**`가 남는지 검사하는 스크립트 추가.

검증 케이스:
- URL 요약 샘플 (`**한 줄 요약**`, blockquote, bullet, 다중 bold span)
- 한 bullet 안의 bold span 2개
- closing `**` 뒤 한국어 조사
- 따옴표/괄호 + closing `**` 뒤 조사
- 영문 복수 suffix (`**result**s`)
- `문장끝**볼드**는` 패턴

실행:

```bash
node scripts/test-markdown-bold-regression.mjs
npx tsc --noEmit
```

결과:

```text
Previous failures : 2
Applied failures  : 0
```
