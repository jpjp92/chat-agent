# 2026년 9월 작업 인덱스

| 날짜 | 날짜 로그 | 관련 기록 | 비고 |
|---|---|---|---|
| 09-01~02 | 별도 DEV 파일 없음 — [DEV_260830 §6.22~§6.31](../08/DEV_260830.md) 에 이어 기록 | [tests/README](../../../../tests/README.md), [DEV_HISTORY](../../../DEV_HISTORY.md#최근-작업-로그) | 논문 카드 후속 배선(`paperFollowup`)·주제 관련도 필터·카드 흡입 168회 측정·라우터 모델 상향·**빈 카드가 답변을 삼키던 3개 층** 수정 |
| 09-02 | [DEV_260902](DEV_260902.md) | [검색 정책 계획](../../../plans/PLAN_SEARCH_POLICY_260815.md), [tests/README](../../../../tests/README.md) | **"검색해"라고 했는데 검색이 안 되던 두 경로** — ① Gemini 의 물리 제약(TIER 400)이 OpenAI 턴까지 눌렀다(게이트 공급자 인지) ② `클로드 skills 관련된 레포 검색` 이 arXiv 논문 카드로 갔다(프롬프트 부정 앵커 + 결정론적 산출물 가드 + 종합단계 웹검색 2차 방어) |
| 09-03 | [DEV_260902 §10~§11](DEV_260902.md) | [REF_SearchRouting §1-4·§2H](../../../guide/REF_SearchRouting.md) | **arXiv 카드가 무관한 논문으로 채워지던 결함** — ① 모델이 덧붙인 범용어가 AND 교집합을 끌고 갔다(도구 설명에서 차단. 코드로 거르는 수정은 복합 개념을 깨뜨려 **되돌렸다**) ② 불용어 제거가 논문 **제목**을 분해해 `Attention Is All You Need` 가 자기 제목으로 안 찾혔다(구절 우선 폴백). 신규 프로브 `live-arxiv-query.mts` 가 ①의 첫 수정이 회귀임을 잡았다 ③ 🔴 **①의 수정이 Gemini 쪽에만 걸려 있었다** — OpenAI 경로는 `local-tool-registry.ts` 에 `query` 설명의 사본을 갖고 있었다(상수로 합침) ④ 제목 질의가 **틀린 논문을 답으로** 냈다 — `ti:` 우선 조회 + `[N](url)` 번호를 URL 로 교정 |
| 09-03 | [DEV_260903](DEV_260903.md) | [auth-mvp-schema.sql](../../../guide/db/auth-mvp-schema.sql), [storage-user-prefix-rls.sql](../../../guide/db/storage-user-prefix-rls.sql) | **`dev` 브랜치 218 파일 보안 검토(읽기 전용, 코드 미수정)** — 신규 취약점 0건이고 인증 축은 순개선(닉네임 유사 인증의 IDOR 삭제). 남은 둘은 기존 결함 — ① 비인증 **SSRF**(`fetch-url`·`proxy-image`): 방어가 hostname **문자열** 정규식이라 `2130706433`·`0x7f000001`·DNS 로 우회 ② 게스트 한도 우회: `session_id` 를 빼면 카운터 트리거가 안 걸린다 |
| 09-04 | [DEV_260903 §3.0·§3.4](DEV_260903.md) | 위와 동일 | ⚖️ **SSRF 절을 두 차례 자체 정정** — **1차**(문서 재검증): ① `proxy-image` 는 full-read 가 **아니다**(content-type 게이트) ② 진짜 full-read 는 `fetch-url:452` ③ 🔴 **"메타데이터 → 크리덴셜" 은 Vercel(Lambda)에서 성립하지 않는다** — 등급의 유일한 근거였는데 환경 확인 없이 플레이북에서 가져왔다 → **High → Medium**. **2차**(TODO 대조 + Node 실측, 아래 감사에서): ④ **10진·16진 IP 우회는 막힌다 — 우회표가 틀렸다** ⑤ 대신 **IPv6 차단 4항목이 죽은 코드** → **"1순위" 철회**(`speech`·`summarize-title` 이 앞선다) |
| 09-04 | [DEV_260904](DEV_260904.md) | [TODO §보안](../../../TODO.md), [PLAN_INDEX](../../../plans/PLAN_INDEX.md) | **md 161개 전수 감사** — 링크 0건 깨짐·시크릿 0건으로 기계적 건강도는 좋았다. 🔴 **수확은 문서끼리의 모순**이었다: 09-03 보안 검토의 SSRF 우회표가 **TODO 의 2026-08-29 실측과 정면 충돌**했고 실측이 맞았다. 실측을 다시 돌리다 **IPv6 차단 4항목이 전부 죽은 코드**(대괄호)임을 발견 → **TODO 에 열려 있던 `::ffff:` 추가 계획이 실행해도 안 통한다.** 무인증 라우트도 TODO 는 6개인데 검토는 2개만 봐서 우선순위가 틀렸다(`speech`·`summarize-title` 이 진짜 1순위). 부수: PLAN_INDEX 5건 등재·docs/README 서버경계 행 신설·월 README 04~07 누락 확인 |
| 09-05 | 별도 DEV 파일 없음 — 계획 개정만 | [PLAN_HARDENING §6](../../../plans/PLAN_HARDENING_260822.md#6-작업-순서), [TODO §P0](../../../TODO.md) | **하드닝 작업 순서 재정렬** — 09-03 검토가 철회한 "SSRF 1순위"를 대체하는 순서를 확정했다(`speech`·`summarize-title` 토큰 검증 → 게스트 저장 실패 시 중단 → SSRF 공통 판정 → `safeFetch` → 원자적 쿼터). IPv6·숫자 IPv4 는 소스 정규식을 Node 로 재확인. **전부 계획이며 미구현** |
| 09-06 | [DEV_260906](DEV_260906.md) | [Gemini 3.8 도입 계획](../../../plans/PLAN_MODEL_3_8_MIGRATION_260906.md) | 3.8 전용 프로브 추가. 유료용 키 호환성 9항목·실제 그래프 12턴·후보 router 26사례 확인. 3.7/3.8 동일 조건 36회씩 모두 구조·값 통과했지만 3.8 시간비 중앙값 1.125·최대 23.817s라 기본 승격 근거 없음. 검색/외부 도구/미디어·3.6 비교는 미실행 |
| 09-13 | [DEV_260913](DEV_260913.md) | [PLAN_HARDENING §P0-2](../../../plans/PLAN_HARDENING_260822.md), [TODO §P0](../../../TODO.md), [docs/README](../../../README.md) | **문서 정리** — 🔴 수확은 또 문서끼리의 모순이었다: **활성 하드닝 계획의 `P0-2`(model allowlist)가 이미 해소된 취약점**인데 §6 의 한 줄 정정이 정정당한 자리에 표시를 안 남겨 여전히 P0 로 읽혔다. 부수: 깨진 링크 **13건**(09-04 감사가 "0건"이라 한 것) 인라인 강등 · TODO §보안이 `⚪ 백로그` 아래 있던 것을 **`🔴 P0`** 로 승격 · 앵커·행번호 검사기 신설(앵커는 0건) |
| 09-13 | [DEV_260913_DEPS](DEV_260913_DEPS.md) | [PLAN_HARDENING §1 #13](../../../plans/PLAN_HARDENING_260822.md), [TODO §P0](../../../TODO.md) | **의존성 보안 업데이트(dev, 미배포)** — Dependabot 25건의 원인은 **5개**였다(같은 xmldom 취약점이 두 사본에 각각 잡혀 부풀어 보였다). `next` 16.2.6→**16.3.5**(Critical 2 마감 — 다만 둘 다 **이 앱에선 성립 안 함**: Windows 아님·`next/image` 사용처 0건) · `kordoc` 3.1.1→**4.13.1**(major, **서버에서 공격자 파일이 파싱되는 유일한 경로**라 타입 계약 고정 + HWPX 런타임 스모크 후 적용). ⚖️ 사전 분석의 *"mammoth xmldom 은 못 고친다"* 는 **틀렸다** — 0.8.x 라인에 수정판 `0.8.15` 가 있었다. 🔴 남은 High 12건은 전부 `@huggingface/transformers`(kordoc 의 **안 쓰는 OCR 선택적 의존**, `range=*` 라 버전으로 안 닫힘) |
| 09-22 | [DEV_260922](DEV_260922.md) | [3.8 도입 계획](../../../plans/PLAN_MODEL_3_8_MIGRATION_260906.md), [프로브](../../../../tests/manual/gemini-3-8/README.md) | **3.6/3.7/3.8 두 축 비교 342회 — 정답률 전부 무승부**(비검색 252/252, grounding 90/90, `citedWrong` 0). 🔴 **[3.7 계획 §2](../../../plans/PLAN_MODEL_3_7_MIGRATION_260817.md)의 3.6 grounding 결함(2/5)이 재현되지 않았다** — 30/30, "검색어 전부 1개" 서명도 소멸(평균 1.43/1.47/1.41). 다만 3.8이 **검색 없이** 3건을 맞혀 문항 자체가 천장. 확정: ① 3.8은 `thinkingBudget` 수용 ② 미등록 3.8 기본 thinking **사고토큰 11,492**(타 셀 0) ③ 지연은 **DEV_260906 §7과 방향 반전**(`3.8/3.7` 1.125→**0.811**). 🔴 유일한 변별은 **단일 `$` 누출 3.6만 4/84** → `singleDollarTextMath:false`라 화면에 LaTeX 원문 노출 |
| 09-24 | [DEV_260924](DEV_260924.md) | [계층화 계획 §10](../../../plans/PLAN_PROMPT_LAYERING_260923.md), [REF_Architecture](../../../guide/REF_Architecture.md#prompt-composition--language) | **프롬프트 계층 분리 0~7단계 — 문구 변경 0건·응답 변화 없음.** 조립 결과가 `generator.ts` 클로저라 관측 불가였던 것을 순수 함수로 떼어내(`assemblePrompt`) 골든을 박은 뒤 옮겼다 → 2~7단계는 **바이트 동일**이 증명된다(의도 19 × 턴 6 × 언어 4). 🔴 수확은 옮기다 나온 것들이었다: ① **소스 grep 49건**이 세 번에 걸쳐 드러났다(문구는 그대로인데 깨짐 — *"프롬프트를 검사한다"* 고 적고 실제로는 파일의 철자를 검사) ② 순서 검사가 **블록을 통째로 지워도 통과**했다(`-1` 이 제일 작다) ③ 논문 정책 두 벌에 **1,693자 글자 그대로 중복** ④ 추출 스크립트가 `\n\n` 을 리터럴로 써서 개행이 글자가 될 뻔한 것을 골든이 `25973≠25971` 로 잡았다. 판정만 하고 **옮기지 않은 것 2건**: `sports` 0자(규칙이 도구 출력에 산다), `paper_search` 장애≠없음(문구가 `error` 필드를 전제하는데 카드 도구들은 `count:0`+notice 라 **발동하지 않는다**. 같은 규칙이 이미 턴 계층에 있다) |

09-01~02 까지는 08-30 에 시작한 외부 소스 작업의 **연속**이라 날짜로 자르지 않고 같은 로그에 이어 적었다.
09-02 의 검색 라우팅 결함은 **새 줄기**여서 날짜 로그를 새로 만들었다.


09-03 은 09-02 로그에 §10~§13 으로 이어 적었다 — 같은 검색 줄기의 **카드 내용** 쪽이라 파일을 자르지 않았다.


09-03 의 보안 검토는 검색 줄기와 **무관한 축**이라 파일을 따로 냈다.

09-04 의 보안 검토 개정은 그 문서의 **자기 정정**이라 새 파일을 만들지 않고 §3 을 개정했다 — 초판 서술을 지우지 않고 §3.0 표에 "초판 / 실제" 로 남겼다.
09-04 의 문서 감사는 **대상이 다르다**(코드가 아니라 docs 전체)라 파일을 따로 냈다.
