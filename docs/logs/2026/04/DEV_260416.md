# 개발 노트 — 2026-04-16

---

# 전체 코드 보안 취약점 감사 (Security Audit)

> 전체 `api/` + `src/` 레이어 대상 코드 리뷰. LangGraph 에이전트 에러 처리(DEV_260415) 이후 남은 취약점 전수 점검.

---

## 취약점 목록

### 🔴 P1 — Critical

#### #1 · `api/auth.ts:52-55` — IDOR (타 유저 프로필 수정)

**현상**: PATCH `/api/auth` 엔드포인트에서 `id`를 클라이언트 body에서 받아 서버 검증 없이 UPDATE 실행.

```ts
const { id, display_name, avatar_url } = req.body;
await supabase.from('users').update(updatePayload).eq('id', id);
// id 소유 여부 검증 없음 → 임의 UUID를 전달하면 타인 프로필 수정 가능
```

**영향**: 다른 사용자의 `display_name` / `avatar_url` 덮어쓰기 가능. 익명 앱이어도 세션 사칭 벡터.

**수정 방향**:
- Supabase RLS `auth.uid() = id` 정책 적용 (근본 해결)
- 또는 서버에서 JWT 토큰 검증 후 요청자 ID와 `id` 비교

---

#### #2 · `api/sessions.ts` — 전 메서드 소유권 검증 부재

**현상**: 모든 HTTP 메서드에서 `user_id` / `session_id`를 쿼리/body에서 그대로 사용.

```ts
// GET: 타인 메시지/세션 열람
.eq('session_id', session_id)   // 소유자 확인 없음
.eq('user_id', user_id)         // 임의 user_id 가능

// DELETE: 타인 세션 삭제
.delete().eq('id', session_id)  // 소유자 확인 없음

// PATCH: 타인 세션 제목 변경
.update({ title }).eq('id', session_id)
```

**영향**: UUID를 알거나 추측할 경우 타인의 대화 내용 열람, 세션 삭제, 제목 변조 가능.

**수정 방향**:
- Supabase RLS `user_id = auth.uid()` 정책이 가장 근본적
- RLS 불가 시 서버에서 `SELECT user_id FROM chat_sessions WHERE id = session_id` 후 요청자와 비교

---

#### #3 · `api/fetch-url.ts:49` — SSRF (Server-Side Request Forgery)

**현상**: 사용자 제공 URL을 내부 IP 차단 없이 `fetch` 실행.

```ts
const response = await fetch(targetUrl);  // IP 검증 없음
```

**영향**: `http://169.254.169.254/latest/meta-data/` (AWS/GCP 메타데이터), `http://localhost:*`, `http://10.x.x.x` (내부망) 접근 가능.
Vercel 서버리스 환경에서도 클라우드 메타데이터 엔드포인트는 접근 가능.

**수정 방향**:

```ts
const BLOCKED_PATTERNS = [
    /^127\./, /^10\./, /^172\.(1[6-9]|2\d|3[01])\./, /^192\.168\./,
    /^169\.254\./, /^0\.0\.0\.0/, /^localhost$/i, /^::1$/
];

function isPrivateUrl(urlStr: string): boolean {
    try {
        const { hostname } = new URL(urlStr);
        return BLOCKED_PATTERNS.some(p => p.test(hostname));
    } catch { return true; }
}

if (isPrivateUrl(targetUrl)) {
    return res.status(400).json({ error: 'Invalid URL' });
}
```

YouTube 분기에서 line 34의 `pageRes` fetch도 동일 guard 필요.

---

### 🟠 P2 — High

#### #4 · `api/upload.ts:18-49` — bucket 화이트리스트 없음 + MIME 타입 미검증

**현상**: `bucket`, `mimeType` 모두 클라이언트 body 값을 그대로 사용.

```ts
const { fileName, fileData, mimeType, bucket } = req.body;
// bucket 미검증 → 프로젝트 내 임의 버킷에 업로드 가능
// mimeType 미검증 → text/html, application/javascript 등 가능
await supabaseClient.storage.from(bucket).upload(filePath, buffer, { contentType: mimeType });
```

**수정 방향**:

```ts
const ALLOWED_BUCKETS = ['chat-imgs', 'chat-videos', 'chat-docs'];
const ALLOWED_MIMETYPES = [
    'image/jpeg', 'image/png', 'image/webp', 'image/gif',
    'application/pdf', 'video/mp4', 'video/webm',
    'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
];

if (!ALLOWED_BUCKETS.includes(bucket)) {
    return res.status(400).json({ error: 'Invalid bucket' });
}
if (!ALLOWED_MIMETYPES.includes(mimeType)) {
    return res.status(400).json({ error: 'Unsupported file type' });
}
```

---

#### #5 · `api/upload.ts:67` — 내부 에러 객체 클라이언트 노출

**현상**: catch에서 Supabase 에러 객체 전체를 `details`로 전송.

```ts
return res.status(500).json({ error: error.message, details: error });
// Supabase 내부 경로, 정책명, 스키마 구조 포함 가능
```

**수정 방향**:

```ts
console.error('[Upload API] Error:', error);
return res.status(500).json({ error: 'File upload failed' });
```

---

#### #6 · `api/auth.ts:65`, `api/sessions.ts:76` — 내부 에러 메시지 노출

```ts
return res.status(500).json({ error: error.message });
// DB 제약조건명, 스키마 정보 등 노출 가능
```

**수정 방향**:

```ts
console.error('[Auth API] Error:', error.message);
return res.status(500).json({ error: 'Internal server error' });
```

---

#### #7 · `api/fetch-url.ts` — 전체 fetch timeout 없음

**현상**: line 29 (YouTube oembed), line 34 (페이지 fetch), line 49 (일반 URL fetch) 모두 `AbortController` 없음. 외부 서버 응답 없으면 Vercel 300s 타임아웃까지 hang.

```ts
const oRes = await fetch(`https://www.youtube.com/oembed?...`);  // line 29, timeout 없음
const pageRes = await fetch(targetUrl);                           // line 34, timeout 없음
const response = await fetch(targetUrl);                          // line 49, timeout 없음
```

**수정 방향**: 각 fetch에 10~15초 `AbortController` 추가. `fetch-transcript.ts`와 동일 패턴.

---

#### #8 · `api/sync-drug-image.ts` — 스크래핑 분기 fetch timeout 없음

**현상**: HTML 스크래핑 후 이미지 fetch 분기(line 190, 200)에 `AbortController` 미적용.

```ts
const scrapedResponse = await fetch(targetScrapedUrl, { headers: {...} }); // timeout 없음
const retryResponse   = await fetch(targetScrapedUrl, { headers: {...} }); // timeout 없음
```

`fetchWithRetry`(line 136)에는 retry 로직만 있고 per-request timeout 없음.

**수정 방향**: 스크래핑 fetch 각각에 8~10초 `AbortController` 추가.

---

### 🟡 P3 — Medium

#### #9 · `api/create-signed-url.ts:9,22` — bucket 화이트리스트 없음 + 파일명 미흡

**현상 1**: `bucket`이 클라이언트 body 값. Admin 클라이언트(`supabaseAdmin`)로 실행되어 영향도 높음.

```ts
const { fileName, bucket } = req.body;
await supabaseAdmin.storage.from(bucket).createSignedUploadUrl(filePath);
// Admin 권한으로 임의 버킷에 서명된 URL 발급 가능
```

**현상 2**: 파일명 정제에서 점(`.`) 허용 → `..` 포함된 경로 생성 시도 가능.

```ts
const safeBaseName = fileName.replace(/[^a-zA-Z0-9.-]/g, '-').toLowerCase();
// '..' → '--' 이지만 timestamp_ prefix로 완화. 그래도 명시적 차단 권장.
```

**수정 방향**:

```ts
const ALLOWED_BUCKETS = ['chat-imgs', 'chat-videos', 'chat-docs'];
if (!ALLOWED_BUCKETS.includes(bucket)) {
    return res.status(400).json({ error: 'Invalid bucket' });
}

const safeBaseName = fileName
    .replace(/\.\./g, '')
    .replace(/[^a-zA-Z0-9_-]/g, '-')
    .replace(/-+/g, '-')
    .slice(0, 100);
```

---

### 🔵 P4 — Low

#### #10 · `src/hooks/useChatStream.ts:266` — setTimeout 미정리

```ts
setTimeout(() => setLoadingStatus(null), 3000); // ID 미저장
```

타이머 ID를 저장하지 않아 컴포넌트 언마운트 후에도 실행 가능. 다수 YouTube 요청 시 타이머 누적.

**수정 방향**:

```ts
const statusTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);

// YouTube 분기에서
if (statusTimerRef.current) clearTimeout(statusTimerRef.current);
statusTimerRef.current = setTimeout(() => setLoadingStatus(null), 3000);

// useEffect cleanup
return () => {
    if (statusTimerRef.current) clearTimeout(statusTimerRef.current);
};
```

---

## 오탐 확인 (False Positive)

| 항목 | 내용 | 결론 |
|------|------|------|
| `sync-drug-image.ts` inflight 메모리 누수 | catch block(line 406-409)에서 `resolveInflight?.(null)` + `inflightRequests.delete` 처리됨 | ✅ 이미 처리 |

---

---

## 노드 버그 점검 — 2026-04-16 (저녁)

> router.ts / vision.ts / generator.ts 전 코드 리뷰.

---

### Bug A — `drug-info-tool.ts`: ConnectDI URL 도메인/포맷 오류

**현상**: MFDS 성공/실패 양쪽에서 AI가 `https://www.connectdi.co.kr/?search=...` URL을 응답에 포함. 해당 도메인은 실제 ConnectDI 서비스가 아님.

**원인**: line 326, 407에 `connectdi.co.kr` (틀린 도메인)과 `?search=...` (존재하지 않는 파라미터) 형식 사용. 실제 서비스 도메인은 `connectdi.com`, 올바른 검색 URL은 `/mobile/drug/?pap=search_result&search_keyword_type=all&search_keyword=...`.

**수정**: 두 곳 모두 올바른 URL 형식으로 교체.

---

### Bug B — `drug-info-tool.ts`: PARTIAL_DATA 케이스 drug card 미생성

**현상**: MFDS 데이터 없음 + pharm.or.kr 발견 시 AI가 drug card 대신 "상세 정보를 찾을 수 없습니다" 텍스트 응답.

**원인**: `PARTIAL_DATA` 지시문이 "json:drug를 생성하세요"보다 `search_web` 호출을 먼저 안내 → AI가 검색 결과 부족 시 포기.

**수정**: PARTIAL_DATA 메시지를 `[MANDATORY] 반드시 json:drug 블록을 생성하세요`로 강화. 부분 데이터만 있어도 drug card를 먼저 생성하고 나머지는 search_web으로 채우도록 순서 변경.

---

### Bug C — `sync-drug-image.ts`: MFDS 200 HTML 유지보수 페이지 처리 누락

**현상**: MFDS 서버 유지보수 시 `200 text/html`(서비스 중단 안내) 응답 → `!externalResponse.ok` 조건 불충족 → ConnectDI fallback 미동작 → 422 반환.

**원인**: line 161의 `!externalResponse.ok` fallback은 404/5xx 전용. 200 HTML은 line 415의 content-type 체크에서 걸리지만 거기엔 fallback 없음.

**수정**: `!contentType.includes('image/')` 체크 블록(line 415)에 MFDS 도메인 감지 + ConnectDI fallback 추가.

```
MFDS 200 HTML → content-type mismatch → MFDS 도메인? → tryConnectDIFallback → 성공 시 200 반환
```

---

### Bug B — `generator.ts`: `[YOUTUBE_VIDEO_INFO]` vs `[TRANSCRIPT]` 불일치

**현상**: YouTube 트랜스크립트가 있을 때 Google Search 비활성화 최적화가 동작 안 함.

**원인**: `useChatStream.ts`에서 트랜스크립트를 `[TRANSCRIPT]`로 포맷팅하지만 `generator.ts`에서 `[YOUTUBE_VIDEO_INFO]`로 체크. `hasTranscript`가 항상 `false` → `useGoogleSearch`가 의도와 달리 `true` 유지.

**수정**: `generator.ts:131` — `'[YOUTUBE_VIDEO_INFO]'` → `'[TRANSCRIPT]'` 로 수정.

---

### Bug C — `generator.ts`: `sdkSuccess` let 블록 스코프 버그

**현상**: 비약품 인텐트(general 등)에서 SDK 경로가 완전 실패(모든 API 키 소진)하면 `if (!useLangChain)` 블록 밖 line 277에서 `sdkSuccess` 접근 시 ReferenceError 발생 가능.

**원인**: `let sdkSuccess = false` 선언이 `if (!useLangChain)` 블록 내부(line 67)에 있어 블록 탈출 후 참조 불가.

**수정**: `sdkSuccess` 선언을 `if (!useLangChain)` 블록 바깥으로 이동.

---

## 우선순위 로드맵

| 순위 | # | 파일 | 작업 | 상태 |
|------|---|------|------|------|
| **P1** | #1 | `auth.ts:52` | PATCH 소유권 검증 or Supabase RLS | ⬜ 미완료 |
| **P1** | #2 | `sessions.ts` | 전 메서드 소유권 검증 or Supabase RLS | ⬜ 미완료 |
| **P1** | #3 | `fetch-url.ts:49` | `isPrivateUrl` guard (SSRF 차단) | ⬜ 미완료 |
| **P2** | #4 | `upload.ts:18` | bucket 화이트리스트 + mimeType 검증 | ⬜ 미완료 |
| **P2** | #5 | `upload.ts:67` | `details` 제거, 에러 sanitize | ⬜ 미완료 |
| **P2** | #6 | `auth.ts:65`, `sessions.ts:76` | 에러 메시지 sanitize | ⬜ 미완료 |
| **P2** | #7 | `fetch-url.ts` | AbortController 10s (3곳) | ✅ 완료 |
| **P2** | #8 | `sync-drug-image.ts` | 스크래핑 fetch timeout (다수) | ✅ 완료 |
| **P3** | #9 | `create-signed-url.ts` | bucket 화이트리스트 + `..` 제거 | ⬜ 미완료 |
| **P4** | #10 | `useChatStream.ts:266` | setTimeout ref + cleanup | ⬜ 미완료 |

---

## 버그 수정 — 2026-04-16 (오후)

> 보안 감사 이후 발견된 기능 버그 2건 수정.

---

### Fix A — 약품 카드 이미지 미표시 (모바일)

**현상**: 모바일에서 약품 카드 이미지가 일부 표시 안 됨. ConnectDI URL인 경우 shimmer 계속 표시.

**원인**: `sync-drug-image.ts` 내부 fetch 전체에 timeout 없음 → 모바일 네트워크 지연 시 Vercel 300s까지 hang. `DrugRenderer.tsx` 클라이언트 fetch에도 timeout 없어 shimmer 영구 표시.

**수정 파일 및 내용**:

| 파일 | 추가된 timeout | 대상 |
|------|--------------|------|
| `proxy-image.ts` | 10s | 외부 이미지 프록시 fetch |
| `DrugRenderer.tsx` | 20s | `/api/sync-drug-image` 클라이언트 fetch → timeout 시 abort → proxy fallback 전환 |
| `sync-drug-image.ts` | 5s | Supabase 캐시 HEAD check |
| | 12s/attempt | `fetchWithRetry` 메인 이미지 다운로드 (AbortError 시 재시도 안 함) |
| | 8s × 2 | pharm.or.kr 이미지 fetch (`_b.jpg`, `_s.jpg`) |
| | 8s | Naver/ConnectDI 스크래핑 fetch |
| | 8s × 2 | `fetchConnectDIDetailHTML` HTML fetch + 상세페이지 fetch |

**효과**: ConnectDI URL 기준 20s 내 sync 실패 시 클라이언트가 자동 abort → `setSyncing(false)` → proxy fallback 또는 no-image placeholder로 전환. Shimmer 영구 표시 해소.

---

### Fix B — 채팅 이전 대화 로드 시 사용자 메시지만 표시

**현상**: 채팅 히스토리 재진입 시 AI 응답 없이 유저 질의만 표시되는 경우 발생.

**원인 분석**:

- 유저 메시지 저장: 스트림 시작 전 fire-and-forget → 스트림 실행 5~30초 동안 완료 ✅
- AI 응답 저장: 스트림 완료 후 fire-and-forget → `res.end()` 직전에 Promise 등록 → **Vercel이 `res.end()` 후 함수 freeze 시 `.then()` callback 미실행** ❌

화면에는 응답이 표시되는 이유: SSE 청크는 스트리밍 도중 이미 브라우저에 전송됨. DB 저장과 무관하게 렌더링됨.

**수정 파일**: `api/chat.ts` — line 279

```ts
// 기존 (fire-and-forget)
supabase.from('chat_messages').insert({...}).then(({ error }) => { ... });
supabase.from('chat_sessions').update({...}).then(({ error }) => { ... });

// 수정 (await — SSE 이미 완료 상태이므로 UX 영향 없음)
try {
    await Promise.all([
        supabase.from('chat_messages').insert({...}),
        supabase.from('chat_sessions').update({...}).eq('id', session_id)
    ]).then(([{ error: msgError }, { error: sessionError }]) => { ... });
} catch (dbError: any) {
    console.error('[Chat API] DB save failed:', dbError.message);
}
```

**효과**: `res.end()` 전에 DB 저장 완료 보장 → 채팅 히스토리 재진입 시 AI 응답 정상 표시.

> **참고**: v4.36(DEV_260413)에서 세션 종료 버그 해결을 위해 도입한 fire-and-forget이 이 문제의 원인. 당시엔 Vercel 60s 한계 회피가 목적이었으나 현재 Fluid Compute에서 기본 timeout이 300s로 증가하여 `await`로 변경해도 무방함.

---

## Fix C — MFDS 이미지 서버 장애 시 ConnectDI 폴백

**현상**: nedrug.mfds.go.kr 서비스 일시 중단으로 MFDS 이미지 URL이 404 반환 → 약품 이미지 전체 미표시.

**원인**: `sync-drug-image.ts`가 외부 이미지 fetch 실패 시 즉시 404를 반환하고 대체 소스를 시도하지 않음.

**수정 파일**: `api/sync-drug-image.ts`

**수정 내용**:

1. **헬퍼 함수 추가**

    | 함수 | 역할 |
    |------|------|
    | `extractBaseName(name)` | 괄호·용량·단위 제거하여 기본 약품명 추출. `\b` 미사용 (한글 뒤 동작 안 함) |
    | `extractDosageNumbers(name)` | 숫자만 추출하여 용량 비교에 사용 |
    | `scoreNameMatch(target, candidate)` | 100(완전일치) / 80(기본명+용량) / 60(기본명) / 40(부분포함) / 0 |
    | `parseMedList(html)` | `<a href="?pap=detail">` 블록에서 `<dt>` 이름 + `/design/img/drug/` 이미지 URL 추출 |

2. **`tryConnectDIFallback` 함수 추가**: `extractBaseName(drug_name)`으로 검색어 생성 → ConnectDI 검색 (10s timeout) → `parseMedList` 파싱 → `scoreNameMatch` 최적 선택(score ≥ 40) → 이미지 다운로드 (8s timeout) → Supabase 업로드 → `pillVisual` 추출

3. **MFDS 실패 시 폴백 호출** (line 161): `nedrug.mfds.go.kr` URL이 non-OK이고 `drug_name`이 있으면 `tryConnectDIFallback` 호출. 성공 시 정상 응답, 실패 시 기존 에러 응답 유지.

4. **기존 ConnectDI 검색결과 파싱 버그 수정** (line 289): `html.split(/<div[^>]*class="[^"]*drug_list[^"]*"/)` → `parseMedList(html)` + `scoreNameMatch` 기반 선택으로 교체. 기존 `drug_list` 클래스는 실제 ConnectDI HTML에 존재하지 않아 항상 빈 배열이었음.

**동작 흐름 (MFDS 장애 시)**:
```
nedrug URL 실패 → tryConnectDIFallback(drug_name)
  → extractBaseName → ConnectDI 검색
  → parseMedList → scoreNameMatch 최적 매칭
  → 이미지 다운로드 → Supabase 캐시
  → pillVisual 추출 → 정상 응답
```

**효과**: MFDS 서버 장애 시에도 ConnectDI에서 동명 약품 이미지를 자동으로 가져와 캐시. 이후 동일 약품은 Supabase 캐시 히트로 MFDS 없이도 정상 표시.

> **주의**: score ≥ 40 미만인 경우 폴백 미적용 (오매칭 방지). ConnectDI에 없는 약품은 기존 동작(404 반환) 유지.
