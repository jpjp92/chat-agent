# DEV_260808 — 업로드 첨부 분석 실패 (영상·대용량 PDF)

> 작성: 2026-08-08
> 증상: 영상 파일을 올리면 **"응답을 받지 못했습니다. 다시 시도해주세요."** / PDF 도 "조금 커지면" 같은 증상
> 성격: 단일 증상 뒤에 **원인 셋**이 겹쳐 있었다. 셋 다 `3.6-flash` 기본 승격(`d439e3d`) 때 생긴 공백이다.

## 요약 (TL;DR)

- **타임아웃이 아니었다.** `gemini-3.6-flash` 는 **임의 URL `fileData` 를 아예 처리하지 못한다**(429 RESOURCE_EXHAUSTED). 업로드 첨부는 Storage 공개 URL 로 나가므로 항상 이 경로다.
- 🔴 **무료티어 한계가 아니다** — **유료 TIER1 키도 동일하게 429.** 티어를 올려도 안 풀린다. (조사 도중 "무료티어 문제"로 단정했다가 정정)
- "타임아웃처럼 보인" 이유: 429 → 키 로테이션 → **12키 × ~12s ≈ 144s** → Vercel 60s 캡 초과 → 텍스트를 한 번도 못 받고 종료. **어떤 키로도 성공하지 못하는 실패를 12번 재시도**하고 있었다.
- "PDF 는 커지면"의 정체는 [useChatStream.ts](../../../../src/hooks/useChatStream.ts) 의 **1MB 임계값** — 넘는 순간 inline → URL 로 경로가 바뀐다.
- 고치는 과정에서 **세 번째 문제**가 드러났다: `SDK_CALL_TIMEOUT_MS = 25s` 가 정상 응답(21~32s)을 끊고 있었다 — YouTube 가 겪었던 회귀와 같은 구조.

---

## 1. 근본 원인 — 3.6 은 임의 URL fileData 를 못 받는다

### 1-1. 계층 분리부터

서버 코드를 읽는 대신 **Gemini 를 직접 쳐서** 계층을 갈랐다. 우리 코드가 원인인지 모델이 원인인지 먼저 정하지 않으면 추측이 된다.

실제 사용자 파일(`reference/테스트.mp4`, 10초·2.76MB)을 dev `/api/upload` 로 올려 **프로덕션과 같은 Supabase 공개 URL** 을 만들고, 그 URL 로 모델 × 입력 유형 행렬을 측정했다:

| | 텍스트 | YouTube | **업로드 영상** | 인라인 이미지 | 400p PDF(URL) | 400p PDF(inline) |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| **gemini-3.6-flash** | ✅ 6.3s | ✅ 7.5s | **🔴 429** | ✅ 4.8s | **🔴 429** | 🔴 503 |
| gemini-2.5-flash | ✅ 1.5s | ✅ 19.7s | **✅ 21.2s** | ✅ 5.6s | ✅ 31.8s | ✅ 21.2s |

### 1-2. 쿼터 소진이 아니다 — 근거 둘

429(RESOURCE_EXHAUSTED)는 보통 쿼터로 읽힌다. 아니었다:

1. **무료 키 4개 교차** — `API_KEY`~`API_KEY4` 전부 *같은 키가* 텍스트엔 200, 업로드 영상엔 429. 직전 호출이 성공하는 키를 "소진"이라 부를 수 없다.
2. **유료 TIER1 키도 429** — 티어 상향으로 안 풀린다.

즉 **모델 능력 한계**다. 그리고 축이 좁다 — 3.6 은 인라인 이미지도, **YouTube `fileData` 도** 정상이다. 막히는 건 **우리 Storage URL 을 `fileData` 로 줄 때**뿐이다.

> ⚠️ 조사 중간에 "무료티어 한계"로 단정하고 코드 주석에도 그렇게 적었다. 유료 키 확인 후 정정했다. **429 를 보면 쿼터로 읽고 싶어지는 편향**이 있고, 그걸 깨는 건 "같은 키가 직전에 성공했는가"라는 대조군이었다.

### 1-3. 왜 이 경로만 뚫려 있었나

[models.ts](../../../../server/models.ts) 의 능력 테이블이 **틀린 주장**을 하고 있었다:

```ts
"gemini-3.6-flash": { freeTierSearch: false, fastMultimodal: true, fastLongInput: true }
//                                            ↑ 업로드 첨부에는 거짓
```

그리고 강등 조건이 **YouTube 전용**이었다:

```ts
const pinYoutube = isYoutubeRequest && hasVideoData && !selCaps.fastMultimodal;
const pinUrl     = hasUrlContentForModel && !selCaps.fastLongInput;
```

업로드 첨부는 `isYoutubeRequest === false` 라 **어떤 핀에도 안 걸린다.** 기본 모델이 2.5 이던 시절엔 문제가 없었고, 3.6 승격(`d439e3d`) 때 함께 넘어오지 않았다.

### 1-4. 조치 — 축을 하나 추가

`freeTierSearch` 가 이미 쓰는 것과 **같은 패턴**이다.

```ts
// models.ts — 티어 무관. 3.x 는 429.
"gemini-3.6-flash": { …, urlFileData: false },
"gemini-2.5-flash": { …, urlFileData: true  },

// generator.ts
const hasUrlFileData = state.messages.some((m) => Array.isArray(m.content) &&
    m.content.some((p) => p.fileData?.fileUri && !/youtube\.com|youtu\.be/.test(p.fileData.fileUri)));
const pinUrlFileData = hasUrlFileData && !selCaps.urlFileData;
const resolvedModel = (pinYoutube || pinUrl || pinUrlFileData) ? SERVER_MODELS.FLASH : sel;
```

---

## 2. "PDF 는 조금 커지면" — 1MB 임계값

같은 뿌리인데 **증상이 크기에 따라 갈리는 이유**가 따로 있었다. [useChatStream.ts:196](../../../../src/hooks/useChatStream.ts#L196):

```ts
// 문서: 1MB 미만 base64 inline 허용 (Vercel payload 4.5MB 제한 우회)
if (!isImage && !isVideo && estimatedSize < (1 * 1024 * 1024) && isBase64) {
  finalAttachments.push(attachment);   // ← inline 유지
  continue;
}
// 그 외 → Storage 업로드 → data = URL
```

| 첨부 | 경로 | 3.6 결과 |
|---|---|---|
| PDF < 1MB | inline base64 | 대체로 성공 (단 페이지가 많으면 §2-1) |
| **PDF ≥ 1MB** | Storage URL → `fileData` | **429 — 항상 실패** |
| **영상 (크기 무관)** | 항상 Storage URL | **429 — 항상 실패** |

### 2-1. 작은 PDF 도 "가끔" 실패하던 이유

400페이지(0.41MB, 입력 103k토큰)를 **inline** 으로 보내면 3.6 이 **503 UNAVAILABLE** 을 낸다. 429 와 달리 503 은 기존 코드가 이미 2.5 로 강등하므로(재시도 23s + 21s ≈ 44s) **60s 안에 겨우 들어와 성공**한다.

→ 그래서 "될 때도 있고 안 될 때도 있는" 증상이 나왔다. **429 는 항상 실패, 503 은 자가 회복.** 두 에러의 처리 차이가 증상 차이를 만들고 있었다.

---

## 3. 🔴 "키 소진"과 "모델 거부"를 구분하지 못했다

### 3-1. 응답에 단서가 없다

먼저 429 본문에 구조적 단서가 있는지 봤다(있으면 휴리스틱이 필요 없다):

```json
{ "error": { "code": 429, "message": "Resource has been exhausted (e.g. check quota).", "status": "RESOURCE_EXHAUSTED" } }
```

`quotaMetric`·`quotaId`·`retryDelay` **전부 없다.** 무료·유료 모두 맨 429다. → 응답만으로는 구분 불가.

### 3-2. 행동으로 구분한다

429 는 두 가지가 같은 얼굴로 온다:

| | 다른 키로 바꾸면 | 올바른 대응 |
|---|---|---|
| ⓐ 그 키의 쿼터 소진 | 성공한다 | 키 로테이션 |
| ⓑ 모델이 요청을 거부 | **똑같이 실패** | **모델 강등** |

기존 코드는 전부 ⓐ 로 처리해 12키를 돌렸다. 구분 기준:

```ts
// 서로 다른 키 2개가 같은 모델에서 429 → ⓑ 로 보고 2.5 로 강등
const modelWide429 = isRateLimit && rateLimitedKeys.size >= Math.min(2, API_KEYS.length);
const justDowngraded = (isUnavailable || isTimeout || modelWide429) && !unavailableDowngrade && isThreeXFlash(resolvedModel);
```

503/timeout 이 이미 쓰던 `justDowngraded` 메커니즘에 **얹기만 했다** — 새 구조를 만들지 않았다.

**임계값 2 의 근거**: ⓐ 를 ⓑ 로 오판해도 비용이 낮다(2.5 도 잘 답하고 더 빠르다). 반대로 ⓑ 를 놓치면 **확정적으로** 60s 를 넘긴다. 비대칭이라 조기 강등이 옳다. 키가 1개뿐이면 로테이션 자체가 불가능하므로 `Math.min(2, API_KEYS.length)` 로 1회 판정한다.

**검증**(핀을 일부러 끄고 429 경로를 태움):

```
Retrying SDK call (attempt 2) reason: 429 | model: gemini-3.6-flash
429 on 2 distinct keys (model-wide, not key quota) on gemini-3.6-flash — downgrading retries to 2.5-flash
Retrying SDK call (attempt 3) | model: 2.5(downgraded)
✅ 43.1s — 정상 답변
```

43s 는 60s 안이지만 여유가 적다. **핀(§1-4)이 1차 방어, 이 강등은 그물**이다.

---

## 4. 세 번째 문제 — 25s 데드라인이 정상 응답을 끊고 있었다

§3 검증 중 발견. 429 강등 후 PDF 가 2.5 에서 **timeout 으로 계속 재시도**했다:

```
attempt 4~8  reason: timeout | model: 2.5(downgraded)
```

`SDK_CALL_TIMEOUT_MS = 25_000` 인데 400p PDF 는 **21~32s** 가 걸린다. 우리가 정상 응답을 끊고 있었다.

**YouTube 가 겪은 회귀와 완전히 같은 구조다**(`aa9d701` 25s 일률 도입 → `80972c8`·`e189104` 로 57s 예산 복원, 메모리 `youtube-call-timeout-profile`). 그때 만든 예산이 **이름 때문에 업로드 미디어에 적용되지 않았다**:

```ts
const attemptTimeoutMs = isYtVideoTurn ? YOUTUBE_CALL_TIMEOUT_MS : SDK_CALL_TIMEOUT_MS;
//                       ↑ 업로드 영상·PDF 는 여기 안 들어온다
```

→ 상수명을 대상에 맞게 고치고 범위를 넓혔다:

```ts
export const HEAVY_MEDIA_CALL_TIMEOUT_MS = 57_000;
/** @deprecated 이름이 대상을 좁게 말한다 */
export const YOUTUBE_CALL_TIMEOUT_MS = HEAVY_MEDIA_CALL_TIMEOUT_MS;

const isHeavyMediaTurn = isYtVideoTurn || hasUrlFileData;
const attemptTimeoutMs = isHeavyMediaTurn ? HEAVY_MEDIA_CALL_TIMEOUT_MS : SDK_CALL_TIMEOUT_MS;
```

데드라인 소진 후 재시도 차단(`ytPrimaryTimedOut`)도 같은 범위로 넓혔다 — 57s 를 쓰고 또 재시도하면 60s 캡을 확실히 넘긴다.

> **이름이 범위를 좁혔다.** `YOUTUBE_CALL_TIMEOUT_MS` 라는 이름 때문에 "YouTube 전용 예산"으로 읽혔고, 같은 프로파일의 업로드 미디어가 조용히 빠졌다. §1-3 의 `pinYoutube` 와 **같은 실수**다 — 특정 사례로 이름 붙인 규칙이 그 사례에만 적용되는 것.

---

## 5. 검증

[scripts/test-video-upload.ts](../../../../scripts/test-video-upload.ts) 신설 — 실그래프(라우터→generator)에 태우고, **강등 로그를 가로채 핀이 실제로 걸렸는지**까지 본다. 55s 초과 시 경고(로컬에서 넘으면 프로덕션에선 확실히 끊긴다).

```
### gemini-3.6-flash   (핀 동작 확인)
  ✅ 업로드 영상 (10s, 2.8MB)     22.4s  → pinned to gemini-2.5-flash
     "한복을 입은 귀여운 표범 캐릭터가 …무술 자세를 취하는 모습이 보입니다."
  ✅ 대용량 PDF (400p, 103k토큰)  13.8s  → pinned to gemini-2.5-flash
     "이 문서는 총 400페이지입니다."

### gemini-2.5-flash   (강등 없음)
  ✅ 18.5s / 12.4s
```

4/4 통과, 전부 60s 캡에 여유. `tsc --noEmit` 0.

### 5-2. dev 배포 후 실제 UI 검증

하니스는 클라 왕복·SSE 를 안 보므로 화면에서 다시 확인했다. **모델 선택은 3.6-flash** — 오늘 고친 강등·타임아웃은 전부 3.6 경로에서만 걸리므로 2.5 로 두고 하면 원래 되던 길을 확인하는 셈이 된다.

| # | 케이스 | 결과 |
|---|---|---|
| 1 | 영상 업로드 (`reference/테스트.mp4`, 10s·2.8MB) | ✅ 한복 입은 아기 표범 / 명상→달리기→도약을 타임스탬프까지 붙여 요약 |
| 2 | YouTube 링크 (회귀) | ✅ 상수 개명이 조건식을 깨지 않았음 |
| 3 | PDF 6.5MB (`Prompt Engineering.pdf`) | ✅ 전체 구조 요약. **다만 체감상 느림 — §5-3** |
| 4 | 수식 (`정규분포 확률밀도함수`) | ✅ 홑달러 유출 0 — §5-4 |

①은 오늘 사용자에게 보이던 증상 그 자체이고, 이제 하니스가 아니라 실제 업로드 경로에서 답이 나온다.

②를 넣은 이유는 `YOUTUBE_CALL_TIMEOUT_MS` → `HEAVY_MEDIA_CALL_TIMEOUT_MS` 개명 때 `isHeavyMediaTurn` 조건식을 함께 건드렸기 때문이다. **원래 되던 것이 깨졌는지 보는 게 새로 고친 것이 되는지 보는 것만큼 중요하다.**

### 5-3. 🔴 6.5MB PDF 는 사실상 캡에 걸려 있다 (실측)

화면에서 통과했지만 "느리다"는 체감이 남았다. **통과 여부만으로는 판단할 수 없다** — 25s 면 정상이지만 50s 면 통과했어도 통과한 게 아니다. 그래서 실제 업로드된 파일(`chat-docs/1786170767135_prompt-engineering.pdf`, 6.5MB)을 하니스 픽스처로 넣고 6회 측정했다. 프롬프트는 UI 의 '문서 분석'과 같은 부하가 되도록 요약으로.

| 회차 | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| 소요 | **54.9s** | 25.3s | 33.2s | 36.4s | 42.1s | 26.9s |

**중앙값 ~35s, 최대 54.9s = 60s 캡의 91%, 편차 2.2배.** 6/6 기능 통과, 6/6 핀 정상(`→ gemini-2.5-flash`).

1회차 54.9s 를 로컬 워밍업(tsx 컴파일·`compileAgentGraph`)으로 의심해 3회를 더 돌렸다. **2차 배치 1회차가 36.4s** 로 나왔으므로 워밍업 아티팩트가 아니라 **정상 분포의 위쪽 끝**이다. 확인하지 않았으면 "첫 회는 원래 느리다"로 넘겼을 값이다.

그리고 이건 **로컬 측정치**다. 프로덕션에는 콜드스타트·SSE·클라 왕복이 더 얹힌다. → **6.5MB PDF 는 지금 "된다"가 아니라 "오늘은 운이 좋았다"가 맞다.** 실제 버킷엔 15.8MB PDF 도 있다.

측정 경위도 남긴다 — `vercel ls` 의 `Duration` 컬럼(56s)은 **빌드 시간이지 함수 실행 시간이 아니다.** 질문에 답이 안 되는 값이라 쓰지 않았다. Hobby 런타임 로그는 실시간 스트림만 잡히고 소급 조회가 안 되므로, **지나간 호출은 다시 잴 수 없다** — 그래서 하니스로 재현하는 쪽을 택했다.

기존 픽스처(400p·103k토큰, 13.8s)가 6.5MB 사례를 전혀 대표하지 못했다는 것도 드러났다.

> ⚠️ **여기서 "토큰 수가 아니라 바이트 크기가 지배 변수"라고 적었는데 틀렸다.** §8-1 에서 크기 3지점을 재보니 크기 비례 성분이 거의 없다(다운로드 전 구간 ~1s, 2.9MB 가 6.5MB 보다 느린 회차 존재). 표본 2개에서 방향을 읽은 것이 성급했다.

### 5-4. 수식 — 구분자 축 재작성이 통했다

[DEV_260805](DEV_260805.md) 에서 프롬프트 규칙을 "내용이 수식이냐"에서 **"구분자를 무엇으로 쓰느냐"** 로 다시 쓴 뒤 첫 화면 확인. 예전 유출 지점이 전부 막혔다:

- 구간·부등식 `$$(-\infty < x < \infty)$$`, `$$\sigma > 0$$` ← 유출 1순위였던 자리
- 맨 변수 `$$X$$` `$$Z$$` `$$\mu$$`
- 순수 숫자 `$$3.14159$$` `$$68.27\%$$`
- 표 셀 안 `$$1$$`, `$$\frac{1}{\sigma\sqrt{2\pi}}$$`

**"수식 같지 않은" 것들이 전부 이중 달러로 나온 게 근거다.** 숫자나 부등식은 모델이 "이건 수식이 아니지" 하고 홑달러로 새던 자리인데, 판단 자체를 묻지 않게 만들자 사라졌다.

---

## 6. 이 세션의 교훈

1. **특정 사례로 이름 붙인 규칙은 그 사례에만 적용된다.** `pinYoutube`·`YOUTUBE_CALL_TIMEOUT_MS` 둘 다 실제로는 "무거운 미디어 턴"의 규칙인데 이름이 YouTube 라 업로드 첨부가 빠졌다. 같은 실수가 **한 파일에서 두 번** 났다.
2. **429 를 쿼터로 읽지 마라.** 대조군("같은 키가 직전에 성공했는가")을 세우기 전엔 알 수 없다. 유료 키 확인 전까지 나는 "무료티어 한계"로 단정하고 있었다.
3. **에러 처리 차이가 증상 차이를 만든다.** 429(강등 없음)는 항상 실패, 503(강등 있음)은 자가 회복 — 그래서 "가끔 된다"로 보였다. 재현이 불안정하면 **에러 코드별 경로**를 먼저 갈라 보는 게 빠르다.
4. **계층을 먼저 가른다.** 서버 코드를 읽기 전에 Gemini 를 직접 쳐서 "모델이 못 하는 것"과 "우리가 잘못하는 것"을 분리했다. 이게 없었으면 타임아웃 튜닝을 하고 있었을 것이다.

---

## 7. 오늘 변경

| # | 변경 | 파일 |
|---|---|---|
| 1 | `urlFileData` 능력 축 신설 (티어 무관 근거 포함) | [models.ts](../../../../server/models.ts) |
| 2 | `pinUrlFileData` — 업로드 첨부 턴 2.5 강등 | [generator.ts](../../../../server/agent/nodes/generator.ts) |
| 3 | 모델 전체 429 판정 → 모델 강등 (키 로테이션 중단) | 〃 |
| 4 | `HEAVY_MEDIA_CALL_TIMEOUT_MS` — 예산 범위를 업로드 미디어로 확장 | 〃 |
| 5 | 회귀 테스트 신설 | [test-video-upload.ts](../../../../scripts/test-video-upload.ts) |
| 6 | **`maxDuration` 60 → 300** (§8-3) | [route.ts](../../../../app/api/chat/route.ts) |
| 7 | `HEAVY_MEDIA_CALL_TIMEOUT_MS` 57s → **90s**, `SDK_CALL_TIMEOUT_MS` 근거 교체 | [generator.ts](../../../../server/agent/nodes/generator.ts) |
| 8 | 3.5 폴백 `abortSignal` 에도 heavy media 축 적용 | 〃 |
| 9 | 지연 계층 분해 하니스 신설 (`scripts/test-pdf-latency.ts`) · `test-video-upload` 에 그래프/모델 분해·`VU_ONLY`·`VU_RUNS` 추가 | — |

---

## 8. 🔴 60s 는 Vercel 의 한계가 아니라 우리가 건 값이었다

§5-3 의 "6.5MB PDF 가 캡의 91%" 를 고치려고 개선안을 찾다가, **전제가 틀렸다**는 걸 뒤늦게 확인했다.

### 8-1. 먼저 지배 변수를 갈랐다 (가설 두 개가 죽었다)

임계값(D3)부터 올리고 싶었지만, **측정 없이 올리면 문제를 옮기기만 한다**. 그래서 `scripts/test-pdf-latency.ts` 로 계층을 분해했다 — 다운로드 / URL fileData / inlineData 를 크기 3지점에서.

1차 측정(`maxOutputTokens: 2048`)에서 inline 이 URL 보다 ~8s 빨라 보였다. **그런데 이 비교는 공정하지 않았다** — 그래프는 문서 턴에 16384 를 쓴다. 조건을 맞춰 다시 재니:

| 크기 | 다운로드 | URL fileData | inlineData |
|---|---|---|---|
| 0.4MB (400p·103k토큰) | 0.8s | 33.1 / 16.1s | 13.2s |
| 2.9MB | 0.9s | 17.5 / 20.7s | 20.5 / 19.0s |
| 6.5MB | 0.7s | 31.9 / 30.4s | 23.9 / 31.9s |

→ **inline 우위는 사라졌다.** 두 경로가 분산 안에서 겹친다. **D3(임계값 상향)로 얻을 게 없다** — 그쪽으로 갔으면 헛수고였다.

동시에 §5-3 에 내가 쓴 **"지배 변수는 바이트 크기"도 틀렸다**. 2.9MB(31.5s)가 6.5MB(24.0s)보다 느린 회차가 있고, 다운로드는 전 구간 ~1s 라 크기 비례 성분이 거의 없다.

### 8-2. 그래프 오버헤드도 아니었다

직접 호출 21~24s vs 그래프 35s 를 보고 "10s 이상이 우리 몫"이라 생각했는데, **이것도 `maxOutputTokens` 혼입이었다.** 하니스에 `Starting SDK stream` 까지의 시간을 찍어 분해하니:

```
그래프 780~1193ms + 모델 25~44s      (3회 일관)
```

**시간의 97% 가 모델 호출이다.** 라우터에 fast-path 를 넣어도 1초 미만이다. (라우터에는 이미 YouTube 전용 fast-path 가 있고 업로드 첨부는 빠져 있다 — §6 교훈 1 의 네 번째 사례지만, **이득이 1s 미만이라 지금 고칠 이유가 없다.**)

### 8-3. 진짜 원인 — `export const maxDuration = 60`

우리가 통제할 수 있는 레버가 없다는 결론에 다다랐을 때, 캡 자체를 의심했다. [route.ts](../../../../app/api/chat/route.ts) 에 `maxDuration = 60` 이 박혀 있고, `fetch-url` 은 이미 `120` 인데 빌드가 통과하고 있었다 — **60 이 진짜 천장이면 그 선언은 성립할 수 없다.**

Vercel 문서(fluid compute 기준):

| 플랜 | 기본 | 최대 |
|---|---|---|
| **Hobby** | **300s** | **300s** |
| Pro / Enterprise | 300s | 800s (확장 1800s) |

**Hobby 도 300s 다.** 60s 는 플랫폼 한계가 아니라 우리가 스스로 낮춘 값이었고, 그 줄이 없었으면 애초에 300s 였다. 비용 측면도 문제없다 — fluid 는 **Active CPU 과금**이라 Gemini 응답을 기다리는 I/O 구간은 CPU 로 잡히지 않는다.

**이 틀린 전제가 코드에 번져 있었다.** `HEAVY_MEDIA_CALL_TIMEOUT_MS = 57_000` 의 근거가 "60s 캡까지 ~3s 여유"였고, 주석에는 **"maxDuration(60s, 무료티어 상향 불가)"** 이라고 단언까지 적혀 있었다. 6.5MB PDF 실측 최대가 54.9s 이므로 **실제 여유는 2s 였다.**

### 8-4. 조치

| 파일 | 변경 |
|---|---|
| [route.ts](../../../../app/api/chat/route.ts) | `maxDuration` 60 → **300** (근거·비용 주석 포함) |
| [generator.ts](../../../../server/agent/nodes/generator.ts) | `HEAVY_MEDIA_CALL_TIMEOUT_MS` 57s → **90s** — 실측 최대 54.9s 의 1.6배, 강등 재시도까지 90×2=180s < 300s |
| 〃 | `SDK_CALL_TIMEOUT_MS` 25s **유지, 근거 교체** — 캡 회피가 아니라 행 감지 UX 가드 |
| 〃 | 3.5 폴백 경로의 `abortSignal` 도 heavy media 축 적용 — **`fileData` 를 싣는데 25s 일률이었다**(같은 결함의 또 다른 사례) |

### 8-5. 재검증

```
업로드 영상 (10s, 2.8MB)      17.2s   그래프 1138ms + 모델 16.0s
대용량 PDF (400p, 103k토큰)   25.5s   그래프  779ms + 모델 24.7s
실사용 PDF (6.5MB)            28.1s   그래프  783ms + 모델 27.3s
6.5MB ×3 반복                 36.7 / 32.2 / 29.6s
```

전부 통과, 핀 3/3 정상. `tsc --noEmit` 0.

⚠️ **이 재검증이 증명하는 것은 "회귀 없음"까지다.** 하니스는 로컬이라 캡이 없으므로 300s 상향의 효과 자체는 증명하지 못한다 — 그건 프로덕션에서 60s 를 넘는 응답이 끊기지 않는 것으로만 확인된다. → §8-7

### 8-7. 프로덕션 실효 확인 (2026-08-09)

배포 후 실제 UI 에서 ① 업로드 영상 ② 6.5MB PDF ③ **11.1MB PDF**(신규) 전부 완주.

**①②는 상향의 증거가 못 된다** — 영상은 17s, 6.5MB 는 중앙값 ~35s 로 원래 60s 안에 들어오던 구간이라 캡을 걷었든 아니든 똑같이 성공한다. 회귀가 없다는 것만 말해준다.

**③이 증거다.** 11.1MB(이전 최대의 1.7배)를 하니스로 3회 재니:

```
#1  71.9s   그래프 1682ms + 모델 70.2s   ← 구 60s 캡이었다면 끊겼다
#2  41.7s   그래프  793ms + 모델 40.9s
#3  48.2s   그래프  741ms + 모델 47.4s
```

**3회 중 1회가 60s 를 넘는다.** 즉 이 크기는 구 캡에서 **상시 실패하는 영역**이고, 프로덕션에서 완주했다는 것은 캡 상향이 실제로 작동했다는 뜻이다.

> 다만 엄밀히는 **사용자 실행이 41s 회차였을 가능성**이 남아 단정할 수 없다. 확실한 것은 "이 크기가 구 캡에서 안정적으로 통과할 수 없다"는 쪽이다. 단발 성공을 증명으로 쓰지 않는다 — 분포 최대가 캡을 넘으면 그것으로 충분하다.

그래프 몫은 여기서도 741~1682ms 로 일정하다(전체의 1~2%). §8-2 의 결론이 크기를 1.7배로 키워도 유지된다.

하니스 경고 임계도 갱신했다 — 240s(300s 의 80%)에서 경고, **60s 초과는 실패가 아니라 "구 캡이었다면 끊겼을 구간"이라는 정보**로 표시한다.

### 8-6. 교훈

**"플랫폼 한계"라고 알고 있는 값이 코드에 리터럴로 박혀 있으면, 한계가 아니라 우리 선택일 가능성이 높다.** 나는 이 60 을 메모리에까지 "상향 불가"로 저장해 두고 있었고, 그래서 **지연 원인을 모델·전송 경로에서만 찾았다.** 두 개의 가설(크기, 그래프 오버헤드)을 죽이고 나서야 캡을 의심했다.

부수 교훈: **비교 측정은 조건을 맞추기 전엔 아무것도 말해주지 않는다.** `maxOutputTokens` 2048 vs 16384 하나 때문에 "inline 이 8s 빠르다"와 "그래프 오버헤드가 10s"라는 결론이 **둘 다** 나왔고, 둘 다 틀렸다.

---

## 9. 남은 것

- [x] dev 배포 후 **실제 UI 에서** 영상 업로드 재확인 → §5-2, 4/4 통과
- [x] ~~6.5MB PDF 의 캡 여유 측정~~ → §5-3, **최대 54.9s (캡의 91%)**
- [x] ~~대용량 PDF 지연을 줄인다~~ → §8. **줄인 게 아니라 캡을 걷었다** — 지연 자체는 Gemini 쪽이라 우리가 줄일 여지가 거의 없었다(그래프 몫 ~1s)
- [x] ~~1MB 임계값 재검토([D3](../../../plans/PLAN_BACKLOG_260801.md))~~ → §8-1 측정 결과 **inline 우위가 없어 무의미**. D3 는 기각
- [x] ~~프로덕션에서 300s 상향 실효 확인~~ → §8-7. **11.1MB PDF 가 로컬 71.9s(3회 중 1회 60s 초과)인데 프로덕션에서 완주**
- [ ] 다른 라우트의 `maxDuration = 60` 도 같은 이유로 낮춰져 있는지 점검(`pill-search`·`showtimes`·`sessions` 등) — 이번엔 `/api/chat` 만 손댔다
- [ ] `3.5-flash` 도 `urlFileData: false` 로 뒀다 — 실측하지 않은 보수적 기본값이다. 3.5 를 쓰는 사용자가 있으면 확인 필요
