# DEV 260621 — World Cup Sports Tool 구현

> 작성일: 2026-06-21
> 설계: `docs/plans/PLAN_WORLDCUP_SPORTS_TOOL_260621.md`
> 구현 계획: `docs/plans/PLAN_WORLDCUP_IMPL_260621.md`
> 진단 배경: `docs/logs/DEV_260620.md` §5
> 테스트 가이드: `docs/guide/REF_Sports.md`

---

## 1. 목적

Gemini grounding은 실시간 스포츠 순위에 구조적으로 부적합(organic 웹페이지만 봄 → 조별 신선도 불균질, 소스 5~25 비결정적, 구글 스포츠 지식 패널 접근 불가 — DEV_260620 §5). 전용 sports API(football-data.org)를 LangGraph tool로 연동해 grounding을 우회.

## 2. 구현 내용

### 신규 파일
- **`lib/sports/football-data.ts`** — football-data.org v4 데이터 계층. `getStandings()`·`getScorers(limit)`·`getMatches({stage,status})`. fetch + 멀티그룹 파싱 + 인메모리 캐시(TTL: standings/scorers 300s, matches 600s, 에러/429 시 stale 폴백) + 마크다운 포맷. 요청 단계 대진이 전부 미정이면 `[NOT_DETERMINED]` 반환.
- **`server/agent/worldcup-tool.ts`** — LangChain `tool`. `resource`(standings/matches/scorers) 인자로 선택적 fetch, `[WORLDCUP_DATA]` + 지시문(데이터만 근거·팀명 한글화·미확정 안내) 반환.

### 수정 파일
- `server/agent/state.ts` — `IntentType`에 `"sports"` 추가.
- `server/agent/graph.ts` — `worldCupTool` import + ToolNode 바인딩.
- `server/agent/nodes/generator.ts` — `LANGCHAIN_INTENTS`에 `"sports"` 추가, `allTools` 분기에 `sports → [worldCupTool]` 바인딩, import 추가.
- `server/agent/nodes/router.ts` — LLM 프롬프트 intent 목록 + `validIntents` + **과거 대회(연도/지난·역대) → general 분기**(API는 현재 대회만, 과거는 학습지식).
- `server/agent/intentRules.ts` — `FALLBACK_RULES`에 sports 키워드 휴리스틱(월드컵/조별/16강/득점왕…).

### 🔴 플랜 갭 발견·수정
구현 계획(Task 3)은 `LANGCHAIN_INTENTS`·ToolNode 등록만 다뤘으나, **generator의 `allTools` 분기(line ~1070)에 sports 바인딩이 빠지면 tool이 LLM에 안 붙어 호출 불가**였음. 실행 중 발견해 `else if (state.intent === "sports") allTools = [worldCupTool];` 추가.

## 3. 검증

- `npx tsc --noEmit` — 전체 타입 에러 0.
- 데이터 계층: `scripts/test-sports-api.mjs`(WC standings 호출), `test-sports-capabilities.mjs`(티어 능력), `test-sports-render.mjs`(표 렌더) — 모두 통과(TIER_ONE, WC 12조 232ms).
- **E2E (dev 서버)**: 5개 시나리오 전부 통과
  1. "월드컵 조별리그 전체 순위" → `intent=sports`, 12조 A~L 일관 표 (grounding 신선도 불균질 해소)
  2. "한국 16강 가능성" → standings 기반 추론
  3. "16강 대진표" → `[NOT_DETERMINED]` 안내 (미정 표 환각 없음)
  4. "2022 월드컵 우승팀" → `Sports: 과거 대회 질의 → general`, 학습지식(아르헨티나)
  5. "득점왕" → Top 10 표, 한글 선수명 번역(데니즈 운다브·메시·비니시우스) + PK·출전수

## 4. 범위/제약 (참고)

- 범위: 월드컵(WC)만. 기능: standings·matches·scorers.
- 제약: 과거 시즌 API 403(→학습지식), rate limit 분당 6회(→인메모리 캐시 필수).
- 비범위(추후): WC 외 리그, 전용 카드 UI(crest 로고/하이라이트), 어시스트·평점(WC 미제공).

---

## 5. (별건) 메가박스 영화 카드 HTML 엔티티 버그 수정

영화 상영표 카드에서 메가박스 영화명·포맷 태그가 `2D&#40;더빙&#41;`·`&#40;더빙&#41; 토이 스토리 5`처럼 HTML 엔티티 원본으로 노출되던 버그. 원인: 메가박스 API의 `f.movieNm`·`f.playKindNm`에 엔티티가 인코딩돼 오는데 디코드 없이 카드로 전달됨(지점 드롭다운은 `lib/theaters.ts`에 디코더가 있어 정상이었음).

수정 `app/api/showtimes/route.ts`: `decodeEntities` 헬퍼 추가(숫자 `&#40;`·16진수 `&#x28;`·named `&amp;` 처리) → 메가박스 `movie`·`format`·`cinema` 필드에 적용. 결과: `2D(더빙)`·`2D(자막)`·`(더빙) 토이 스토리 5` 정상 표시. tsc 0, dev E2E 확인.

---

## 6. (별건) kordoc HWP 문서 파싱 연동 — P1~P3 구현

> 계획: `docs/plans/PLAN_KORDOC_INTEGRATION_260620.md` (P0 검증은 2026-06-20 완료)
> 업로드 타입 종합 가이드: `docs/guide/REF_Document.md`

### 목적
HWP 계열 문서를 **구조 보존 마크다운(진짜 표 포함)**으로 추출해 요약 정확도 향상. 기존 `.hwpx`는 JSZip `<hp:t>` 텍스트만 추출해 셀 경계 소실(표→평문 뭉개짐), `.hwp`/`.hwp3`/`.hwpml`은 미지원이었음.

### 사전 점검
- **npm audit**: 12건(7 mod·4 high)은 전부 기존 의존성(`ws`/`xlsx`/`uuid`/langgraph 등). **kordoc 의존성(cfb/xmldom/jszip 등)은 취약점 0건** → 보안 부채 추가 없음.
- **Supabase 리전**: `cf-ray ...-ICN`(서울 엣지)·gateway direct. ≤4MB 직행이라 영향 미미.

### 구현
- **신규 `app/api/parse-document/route.ts`** — kordoc 파싱 라우트. 입력 2종: ⓐ multipart raw(직행, raw≤4MB) ⓑ `{filePath}`(Storage download, 대용량). `SUPPORTED=['.hwp','.hwpx','.hwp3','.hwpml']`(PDF native/docx·xlsx 현행 유지), `runtime='nodejs'`, `preferredRegion='icn1'`, `maxDuration=60`. 응답 `{markdown,truncated,fileType,blockCount,tableCount,metadata}`(blocks[] 미반환, L5). 길이 상한 `MARKDOWN_MAX=100,000`자 초과 시 트렁케이트(L4).
- **`components/ChatInput.tsx`** — `parseViaKordoc(file)` 헬퍼(4MB 임계값 라우팅: 직행 multipart / create-signed-url→Storage PUT→`{filePath}`). HWP 분기를 `isHwpFile` 4종으로 교체(kordoc 우선 + `.hwpx` JSZip 폴백). accept·드롭 허용목록·아이콘 매핑 4종 반영.
- **`next.config.ts`** — `serverExternalPackages: ['kordoc']`.

### 🔴 빌드 갭 발견·수정
`npm run build`에서 webpack이 kordoc 번들링 중 내부 `pdfjs-dist/legacy/build/pdf.worker.mjs`(PDF 경로용·미설치) 정적 import를 해석하려다 실패. node 런타임 `import('kordoc')`는 정상(HWP 경로만 사용)이므로 `serverExternalPackages`로 번들 제외해 해결. → 빌드 성공(라우트 13→14).

### 🔴 보안 리뷰 대응 (IDOR — 자동 리뷰 지적)
`{filePath}` Storage 경로가 attacker-controlled인데 service-role로 다운로드+삭제 → chat-docs 내 임의 HWP 파일 **유출·파괴** 가능. 조사 결과 앱 전체에 인증 레이어가 없고(미들웨어 없음, upload/create-signed-url 모두 비인증) chat-docs는 유저별 prefix 없는 평면 네임스페이스라, 리뷰가 제안한 "세션 JWT + 유저별 prefix + RLS 유저 스코프"는 이 라우트 하나로 달성 불가(앱 전역 인증 도입은 별건). 무인증 환경의 비례적 조치 2종 적용:
1. **filePath 형식 엄격 검증** — `^\d+_[a-z0-9._-]+\.(hwp|hwpx|hwp3|hwpml)$`(create-signed-url 산출 형식). 슬래시·`..`·미지원 확장자 거부 → traversal/타 객체 접근 차단. (검증 8케이스 통과)
2. **route-side `remove()` 제거** — 소유권 검증 불가라 정리용 delete가 임의 삭제 벡터가 됨. 고아 정리는 버킷 TTL/스케줄로 위임.

### 검증 (dev E2E, localhost — 보안 수정 전 측정, 파싱 동작 동일)
- **직행 multipart**(데이터바우처 1.71MB hwp): 151블록·22표·11,626자, `<table>` 보존 ✅
- **Storage 경유**(signed-url→PUT→filePath): 동일 파싱 결과 ✅ (수정 후 정리는 route에서 안 함)
- **트렁케이트(L4)**(NIA 159K자): `truncated:true`, 100,055자에서 절단 + 안내 문구 ✅
- **미지원 포맷**(.docx): 415 ✅
- **filePath 검증 regex**: 정상 2 / traversal·슬래시·timestamp없음·미지원확장자 6 = 8케이스 전부 기대대로 ✅
- `npx tsc --noEmit` 0, `npm run build` 성공.

### 실사용 E2E (사용자 테스트, 2026-06-21)
- **직행**(데이터바우처 1.7MB hwp): `parse-document 854ms`, 표 수치(64.52%·56.6% 등) 보존된 구조화 요약 ✅
- **Storage 경유**(데이터바우처 10.6MB hwp): `create-signed-url 237ms → parse-document 1660ms` 자동 분기, 심사위원 수준 리뷰(참여율 0% 오타·일정표 누락·개인정보 검토·RTX3090/NAS120TB·체크리스트 표) ✅

### 잔여 백로그
- **chat-docs 고아 파일 정리** — route-side delete 제거에 따라 버킷 TTL 또는 스케줄 정리 필요(대용량 경로만 잔존).
- **앱 전역 인증 + 유저별 storage prefix** — IDOR 근본 해결. 현재는 형식 검증으로 blast radius만 제한.
- Supabase origin 리전 대시보드 최종 확인(ap-northeast-2 기대).

---

## 7. (연계) 첨부 문서 search-gate — grounding 기본 off

### 배경
kordoc 실사용 테스트 중 발견: 문서 요약/검토 요청인데 `intent=general, needsSearch=true → useGoogleSearch=true`로 그라운딩(two-track 2.5+search→3.5)이 발동해 응답 15~19초 + 불필요한 웹검색. 첨부 문서가 답변 근거인데 웹을 또 뒤지는 건 레이턴시·비용·환각 모두 손해.

### 우선순위 (사용자 정의)
1. 첨부 문서 있으면 grounding **off** 기본.
2. 단 사용자가 **문서 외 추가 검증**(검색/조사/최신/출처 등)을 명시 요청하면 **on**.

### 구현 ([server/agent/nodes/generator.ts](../../server/agent/nodes/generator.ts) renderer 게이트 직후)
- `hasDocContent` = `state.webContent`에 `[EXTRACTED_CONTENT:]`(현재 턴 첨부) / `[PREVIOUSLY_UPLOADED_DOCUMENT_CONTENT:]`(후속 턴, lastActiveDoc) 마커 존재.
- `hasDocContent && !explicitSearchRequested` → `useGoogleSearch=false`. 이미 false면 아래 general 게이트(`intent==='general' && useGoogleSearch`) 스킵 → 비활성 확정.
- `explicitSearchRequested`는 기존 정규식 재사용(`검색|찾아|조사|출처|근거|최신|최근|실시간|뉴스|latest|recent|search|source|cite`) — `검토`/`정리`/`요약`은 매치 안 됨.
- 기존 URL 게이트와 동일 철학(소스 있으면 off, 새 검색요구면 열어둠). PDF는 멀티모달이라 `[EXTRACTED_CONTENT:]` 미생성+`hasMultimodalContent`로 이미 off → 충돌 없음.

### 검증
- 게이트 결정 7케이스: 문서+검토/요약/정리 → OFF, 문서+최신/검색/출처 → 열림, 일반질의 → 기존. 전부 기대대로.
- **실사용**(10.6MB hwp + "문서 검토해줘"): `Doc content present ... suppressed` + `useGoogleSearch: false` + two-track 로그 소거 → 단일 패스 14.0s. 문서 근거만으로 상세 리뷰.
- `npx tsc --noEmit` 0.

---

## 8. (연계) DOCX 표 보존 — mammoth `convertToHtml` 전환

### 배경
REF_Document 정리 중 docx가 표를 못 읽는 것 확인(`extractRawText`가 셀을 평문으로 뭉갬). mammoth 자체는 표를 읽으므로 방식만 바꾸면 됨.

### 검증 (`scripts/test-docx-table.mjs`, fixture `reference/docx_test/Claude Opus 문서 테스트.docx` 40.9KB, 표 2개)
- `extractRawText`(현재): 3,209자, 표 평문 ❌
- **`convertToHtml`: 4,331자, `<table>`×2·`<tr>`×10·`<td>`×26·`<th>`×7 완전 보존 ✅** (kordoc HWP와 동일 `<table>` 구조)
- `convertToMarkdown`: 표 0행 ❌ (mammoth 마크다운 라이터는 표 미지원 — 답 아님)
- **레이턴시**(10회): extractRawText 64ms avg / convertToHtml 82ms avg → +~18ms(노이즈 수준). 첨부 시점 추출이라 응답 critical path 밖, 다운스트림 +35%자(≈+400토큰)는 표 인식 대가로 무시 가능.

### 구현
[components/ChatInput.tsx](../../components/ChatInput.tsx) docx 분기 `mammoth.extractRawText` → `mammoth.convertToHtml`(`.value` HTML). 브라우저도 `{ arrayBuffer }` 동일 지원. tsc 0.
- `Unrecognised paragraph style: 'Title'/'Subtitle'` 경고는 무해(내용 추출 영향 없음).

---

## 9. (연계) PPTX 표 보존 — `<a:tbl>` 구조 파싱

### 배경
PPTX는 변환 라이브러리가 없어(mammoth=docx 전용, kordoc=pptx 미지원) `<a:tbl>`을 직접 파싱해야 함. 기존 코드는 슬라이드 XML의 `<a:t>`를 전부 평면 join → 표 구조 소실.

### 구조 파악 (`scripts/test-pptx-table.mjs`, fixture `reference/pptx_test/AgenQ_260513.pptx` 3.8MB·20슬라이드, 표는 슬라이드 20에 1개)
- 표 구조: `<a:tbl>`→`<a:tr>`(행)→`<a:tc>`(셀)→`<a:txBody>`의 `<a:p>`/`<a:t>`. 이 fixture는 3×5 clean grid(병합 셀 0).
- **🔴 추가 버그 발견**: 기존 `<a:t[^>]*>(.*?)</a:t>` 정규식의 `.`이 **개행 포함 런을 통째로 누락**(`<a:t>01\n데이터 규격화</a:t>` 같은 셀이 빈칸 → 표 R0/R1 헤더·설명행 전부 소실). 표뿐 아니라 일반 슬라이드 다줄 텍스트에도 영향. → `[\s\S]`로 교체해 동시 수정.

### 구현 (A안, 신규 의존성 0)
[components/ChatInput.tsx](../../components/ChatInput.tsx) pptx 분기: 슬라이드 XML을 `split(/(<a:tbl…<\/a:tbl>)/)`로 표/비표 분리 → 표는 `<a:tr>/<a:tc>` 파싱해 마크다운 표(ragged 행 패딩), 비표는 `[\s\S]` 런 텍스트. 읽기순서 유지, `[Slide N]` 헤더·빈 슬라이드 안내 유지.
- **병합 셀**(gridSpan/rowSpan/hMerge/vMerge): 마크다운 한계로 근사(빈 셀). self-closing `<a:tc/>`는 매칭 제외 → 병합 많은 표는 컬럼 밀림 가능(복잡 표는 PDF 권장).

### 검증
- A안 실측: 슬라이드 20 → 깔끔한 3×5 마크다운 표 + 제목/캡션 읽기순서. 현재 방식은 표 한 줄 뭉갬 + R0/R1 누락.
- 레이턴시(20슬라이드 전체 20회): flat 6.0ms → A안 8.56ms(**+2.5ms 무시 가능**).
- `npx tsc --noEmit` 0.

---

## 10. generator.ts 리팩토링 — 순수함수·검색게이트·에러분류·LangChain경로 모듈 분리 (move-only)

### 배경
[generator.ts](../../server/agent/nodes/generator.ts)가 1148줄 단일 클로저 함수로 SDK 경로·LangChain 경로·검색 게이트·재시도가 한 함수에 응집 → 가독성/테스트 난항. 버그픽스 주석(Fix A/B·tool_code·MALFORMED 등)이 빼곡한 **행동-임계** 코드라, **동작 변경 0의 순수 이동(move-only)** 만 단계적으로 진행. 계획: `docs/plans` 외 세션 플랜(golden-booping-bumblebee).

### 1순위 — 순수함수 3모듈 (closure 비의존)
- **[pill-messages.ts](../../server/agent/nodes/pill-messages.ts)** (무위험·순수이동): 알약 메시지 헬퍼. export `pillNoMatchMessage`·`pillLookupErrorMessage`·`extractPillMatchType`·`pillCandidateTableMessage` + 내부 `formatPillAttributes`·`parsePillCandidates`. (LangChain drug_id 경로에서 사용)
- **[sdk-contents.ts](../../server/agent/nodes/sdk-contents.ts)** (인자계약): `buildSdkContents(messages, forceTextOnly)` → `{sdkContents, hasMultimodalContent, hasDocumentContent}`. 멀티모달 매핑(inlineData/fileData/image_url·mimeType 힌트) 이동.
- **[generation-config.ts](../../server/agent/nodes/generation-config.ts)** (인자계약): `resolveMaxTokens(...)`·`resolveThinkingConfig(...)`. rendererIntents Set은 모듈 내부 상수화.

### 2순위 — 검색 게이트 추출
- **[search-gate.ts](../../server/agent/nodes/search-gate.ts)**: `decideGoogleSearch(ctx)` — 130줄 grounding 결정 로직(multimodal/youtube/url/renderer/doc/general 게이트 우선순위). intentRules(`classifySearchNeed`·`isFollowupReference`·`shouldSuppressSearchForFollowup`) import도 이 모듈로 이동.
- **입력 계약**: `{webContent, messages, intent, needsSearch, hasMultimodalContent, dropImageForSearch, isYoutubeRequest, hasVideoData, latestUserText}`.
- **반환 계약**: `{useGoogleSearch, hasUrlContent, historyHasImage, rendererIntents, explicitSearchRequested}` — 다운스트림(토큰예산·진단로그)이 소비하는 side-value 동봉. 게이트 내부 전용(hasTranscript·hasVideoSummary·hasDocContent·historyHasUrl·currentMsgHasNonYtUrl)은 비반환. 게이트 내 console.log는 verbatim 동반 이동. `useGoogleSearch`는 게이트 이후 재할당 없음 확인 → `const` 구조분해.

### 4-A순위 — 에러 분류/로테이션 공통화
- **[retry.ts](../../server/agent/nodes/retry.ts)**: SDK catch와 LangChain catch의 **byte-identical 중복분**만 추출 — `isTimeoutError(err)`·`isAuthError(err)` + `markRateLimitKey(key, err)`(daily quota→exhausted / else rate-limited). config(`markKeyRateLimited`·`markKeyDailyExhausted`·`isDailyQuotaError`) import은 retry.ts로 이전(generator의 미사용 `markKeyRateLimited` import 제거).
- **드리프트분은 비추출**: `isRateLimit`/`isUnavailable`은 두 경로 술어가 다름(SDK=message 검사 / LangChain=statusText), `isStreamError`는 LangChain 전용 → 각 site 잔류. 수렴 통합은 동작 변경이라 4-B로 분리(보류).
- **검증**: retry.ts 술어 vs 커밋 전 인라인 byte-identity diff 일치(`isTimeout`·`isAuth`·`markRateLimitKey` 본문). config가 `server-only` 의존이라 tsx 동등성 하니스엔 미포함 → diff로 증명. 순서 근거: 중복은 두 catch가 같은 파일에 있는 지금 통합이 효율적(3-A/3-B 분리 전). 계획 [PLAN_GENERATOR_REFACTOR_260621](../plans/PLAN_GENERATOR_REFACTOR_260621.md).

### 3-A순위 — LangChain 경로 분리
- **[langchain-path.ts](../../server/agent/nodes/langchain-path.ts)**: `runLangChainPath(args)` — SDK실패 YT가드 + fast-pass 6종(pharmacy/hospital/vet/law/movie/drug_id) + direct pill lookup + tool 바인딩·invoke + 키회전 재시도(L682~881). 제어흐름 전부 terminal(return/throw)이라 generator 꼬리는 `return await runLangChainPath(...)` 한 줄.
- **입력 계약 9**: `{state, finalInstruction, resolvedModel, apiKey, systemInstructionBase, useLangChain, sdkSuccess, isYoutubeRequest, hasVideoData}`. `finalInstruction`은 direct pill lookup에서 append되므로 인자로 받아 지역 재할당(caller가 이후 미사용→반환 불요).
- **import 이동**: ChatGoogleGenerativeAI·tool 8종·SystemMessage·HumanMessage·pill-messages 4종을 langchain-path로 이전(generator에서 제거). AIMessage·retry 3종·markKeyInvalid·getNextApiKey·API_KEYS·SERVER_MODELS는 양쪽 공유.
- **검증**: 옮긴 200줄이 커밋 전 generator(HEAD) 대비 whitespace-normalized **완전 일치**(move-only 증명). tsc 0, 1·2순위 동등성 19,423 회귀 0 fail. **dev E2E**(localhost:3001 curl): drug_info(json:drug 카드+툴루프+done) · pharmacy_search(json:pharmacy fast-pass) · sports(worldCupTool) · general(SDK 경로 회귀) 전부 정상. drug_id 직접 pill lookup 분기는 이미지 업로드 필요로 브라우저 잔여 체크.

### 동작 보존 포인트
- `hadMultimodalContent`(catch의 500-재시도용 closure var)는 generator 잔류 → `buildSdkContents` 결과로 `if (hasMultimodalContent) hadMultimodalContent = true` 미러링(루프 내 set 분기와 동치).
- `effectiveMaxTokens`·`effectiveModel`·`is3xModel`(검색게이트 의존)은 generator 잔류, 추출 함수 결과만 소비.
- `extractTextContent`(latestUserText 전용)는 generator 잔류. `matchLabel` 미사용 변수까지 원본 그대로 보존.

### 검증
- 단계별 `npx tsc --noEmit` 0.
- **동등성 테스트** `scripts/test-generator-refactor.mts`(gitignored, tsx): 추출 모듈 vs 커밋 전 인라인 로직 deep-equal. 1순위 223 + 2순위 search-gate 19,200 케이스(webContent 6 × intent 5 × userText 4 × 메시지형태 5 × 플래그 2⁵) = **19,423 pass, 0 fail**.
- generator.ts **1148 → 685줄**(-463, -40%). diff: 1순위 +13/-152, 2순위 +14/-128, 4-A +8/-20, 3-A move ~200줄→호출 1개. 3-A는 dev E2E 4종 통과로 검증.

### 후속 (out of scope)
3-B SDK 경로 분리 → (선택)4-B 에러 술어 수렴 통합. 확정 순서·근거: [PLAN_GENERATOR_REFACTOR_260621](../plans/PLAN_GENERATOR_REFACTOR_260621.md).

---

## 11. 버그픽스 — drug_info OCR 토큰 누출 (텍스트 질문에 "JPW" 등 노출)

### 증상
이미지 없이 텍스트로 약품 설명만 요청("파모티딘 설명해줘")했는데 답변 맨 앞에 `JPW` 같은 무관한 토큰이 붙음(3-A E2E 중 발견, **pre-existing**).

### 근본 원인 (systematic, 진단 로그로 실증)
- [drug-info-tool.ts](../../server/agent/drug-info-tool.ts) `extractImprintViaVision`: MFDS DB 각인이 placeholder `"마크"`인 약품은 알약 이미지를 **LangChain `ChatGoogleGenerativeAI.invoke()`** 로 OCR. 이 중첩 LLM이 `tools` 노드 안에서 실행됨.
- [chat/route.ts](../../app/api/chat/route.ts)의 `on_chat_model_stream` 포워딩이 **denylist(`langgraph_node === 'vision'`만 차단)** 라 `tools` 노드의 중첩 OCR 토큰("JP"/"W")이 부모 SSE로 누출 → 실제 `json:drug` 답변 앞에 노출.
- 진단 로그 실증: `node="tools"`→`"JP"`/`"W"`, `node="generator"`→`"```json:drug…"`. 각인이 실제값인 약품(타이레놀 TYLENOL·엔테론정 E50)은 `needsVision=false` → 누출 없음(증상 비발생이 원인 입증).

### 수정
chat 라우트 스트림 게이트를 **allowlist**로 전환: `if (langGraphNode !== 'generator') continue;` — 사용자 prose는 최종 generator 노드 토큰만 전달, vision·tools중첩·router LLM 전부 배제. 안전망: `on_chain_end==='generator'` 폴백 존재.

### 검증
파모티딘 재현 → 스트림이 곧바로 `\`\`\`json:drug`로 시작(누출 소멸), 타이레놀 회귀 정상, tsc 0. (generator 리팩토링과 무관한 별도 커밋.)

### 추가 — 각인 OCR 로고 오판 개선 ([drug-info-tool.ts](../../server/agent/drug-info-tool.ts))
- **문제**: MFDS DB 각인이 placeholder "마크"(로고형)인 약품을 `extractImprintViaVision`가 OCR할 때, 프롬프트가 *"로고도 가장 유사한 알파벳으로 표현하라"*고 강제 → 곡선 로고를 "M"/"W"/"JP"로 **자신 있게 오판**(M/W는 180° 뒤집힌 동형이라 특히 취약), temp 0.1이라 실행마다 흔들림. 약품 식별 맥락에서 "틀린 각인 단정"은 위험.
- **수정** (사용자 설계): ① 프롬프트를 "명확한 숫자·알파벳만 그대로, 로고·애매한 기호·방향따라 달라보이는 건 `마크` 출력"으로 전환(억지 추측 제거) ② Vision이 `마크`/`없음` 반환 시 override 안 함(DB placeholder 유지) ③ temp 0.1→0(결정성).
- **검증**: 위더스파모티딘정(곡선 로고) → Vision raw `"마크"` → 카드 `imprint_front: "마크"`, 뒷면 명확한 숫자는 `"2"` 그대로. 다른 후보의 실제 "JP" 각인은 정상 override. tsc 0.
