# DEV_260613

> 미사용 의존성 점검·제거 (의존성 정리). 영화 통합/멀티턴 후속: [DEV_260612](DEV_260612.md).

## 1. 미사용 패키지 점검

`package.json` 전 의존성을 코드베이스 전수 grep으로 대조. scraped 산출물(`scripts/movie-spike-output`)·docs·package-lock 제외, 전 확장자(`.ts/.tsx/.js/.jsx/.mjs/.html/.css/.json/.config`) + 동적 import·`require()`·CSS import까지 확인.

| 패키지 | 판정 근거 |
|------|------|
| `@fortawesome/fontawesome-svg-core` | import 0건 (4종 합 ~21M) |
| `@fortawesome/free-regular-svg-icons` | import 0건 |
| `@fortawesome/free-solid-svg-icons` | import 0건 |
| `@fortawesome/react-fontawesome` | import 0건 |
| `react-apexcharts` | `ChartRenderer.tsx`가 vanilla `apexcharts` 직접 사용(`new ApexCharts`), React 래퍼 미사용 |
| `youtube-transcript` | 자막 추출 기능 전면 제거됨(과거 로그) → import 0건 |
| `xml2js` (dev) | import 0건 |
| `@types/jszip` (dev) | jszip가 자체 타입(`index.d.ts`) 번들 → 중복 스텁 |

> `dotenv`는 앱 코드 미사용이나 `scripts/` 테스트 다수가 사용 → 유지(향후 devDependencies 이동 검토).

## 2. 제거 + 검증

```bash
npm uninstall @fortawesome/fontawesome-svg-core @fortawesome/free-regular-svg-icons \
  @fortawesome/free-solid-svg-icons @fortawesome/react-fontawesome \
  react-apexcharts youtube-transcript xml2js @types/jszip
```

- deps 28→**26**, devDeps 10→**8**, node_modules **~25M 절감**(fortawesome 21M 포함).
- `npm run build` ✓ — 13개 라우트 정상 생성, 정적 페이지 13/13 통과. 깨진 import·타입 오류 0.

## 3. 정상 사용 확인(유지)

@google/genai · @langchain/{core,google-genai,langgraph} · @supabase/supabase-js · apexcharts · astronomy-engine · framer-motion · jszip · katex · mammoth · ngl · react-markdown · react-syntax-highlighter · rehype-katex · remark-gfm · remark-math · server-only · smiles-drawer · xlsx · @notionhq/client · 빌드툴(next/react/tailwind/postcss/autoprefixer/typescript/@types/*).
